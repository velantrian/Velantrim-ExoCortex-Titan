# Compatibility install entrypoint — Velantrim Titan 9.0
#
# Dependency authority lives in pyproject.toml and uv.lock.
# Do not add independent version constraints here: doing so previously allowed this
# file to drift from the actual dev/runtime dependency graph (including stale pytest,
# pytest-asyncio, and Kuzu constraints after the LadybugDB migration).
#
# PyYAML is a required production-profile verification dependency, but its version is
# declared only by the `dev` extra in pyproject.toml; it is intentionally not pinned here.
#
# This file is retained only for tools or contributors that still invoke:
#
#     pip install -r requirements-dev.txt
#
# It delegates resolution to the canonical project extras instead of maintaining a
# second hand-written dependency list.

-e .[server,dev,graph-embedded]
