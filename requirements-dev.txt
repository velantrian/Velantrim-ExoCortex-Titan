# Dev / CI — Velantrim Titan 9.0
pytest>=8.0,<9
pytest-asyncio>=0.23,<1
pytest-cov>=4.0,<8
pytest-timeout>=2.3,<3
mypy>=1.10,<2
# Pinned to the exact patch version (Claude audit 2026-07-28, Low): a range
# lets an ambient newer install silently diverge from CI/what's actually
# been verified clean — ruff 0.15.8 produced 141 false-positive UP045
# findings on this same codebase, which reads as "141 new problems" if you
# don't already know to check the installed version first.
ruff==0.4.10
types-PyYAML>=6.0.12,<7
# PyYAML itself (not just its stubs): scripts/validate_production_profile.py and
# tests/test_production_profile.py parse docker-compose.prod.yml. types-PyYAML is
# type stubs only, so without this the validator raised ModuleNotFoundError and
# the production-profile tests silently skipped (PR #63 review).
pyyaml>=6.0
fastapi>=0.110
uvicorn[standard]>=0.27
python-dotenv>=1.0
pydantic>=2.0
httpx>=0.27
aiosqlite>=0.19
# L3 Kuzu (Windows: VC++ Redistributable x64)
kuzu>=0.7,<0.12
# core/file_parsers/archive_parser.py's .rar path is optional at runtime
# (RARFILE_AVAILABLE gate), so without this package its regression tests
# (incl. the H4 zip-bomb streaming-cap fix) silently skip via
# pytest.importorskip — same class of gap as the pyyaml case above.
rarfile>=4.0,<5
