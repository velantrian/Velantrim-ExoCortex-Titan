# BATCH 512: Computer Science — DP, Greedy & String Algorithms

**KnowledgeUnits:** 50  
**Namespace:** `cs.algorithms.dp.*` `cs.algorithms.greedy.*` `cs.algorithms.string.*`  
**Scope:** knapsack, lis, lcs, edit_distance, matrix_chain, greedy_choice, huffman, activity_selection, kmp, rabin_karp, z_algorithm, suffix_array, aho_corasick.

| ID | KnowledgeUnit | Тип | Суть | Практический смысл |
|---|---|---|---|---|
| cs.algorithms.dp.optimal_substructure | Optimal Substructure | INVARIANT | Оптимальное решение задачи содержит оптимальные решения подзадач. Необходимое условие применимости DP. | Ключевое свойство для DP. Если его нет — DP неприменим. |
| cs.algorithms.dp.overlapping_subproblems | Overlapping Subproblems | PROCESS | Одни и те же подзадачи решаются многократно. DP сохраняет результаты и переиспользует (мемоизация). | Без перекрытия — divide & conquer. С перекрытием — DP. Контрольный вопрос: строится ли дерево рекурсии вширь? |
| cs.algorithms.dp.top_down_vs_bottom_up | Memoization vs Tabulation | VARIANT | Top-down (мемоизация): рекурсия + кэш результатов. Bottom-up (табуляция): итеративное заполнение таблицы от базовых случаев. | Мемоизация проще для сложных зависимостей. Табуляция экономит стек и константу. |
| cs.algorithms.dp.knapsack_01 | 0/1 Knapsack | METHOD | Каждый предмет либо берём, либо нет. dp[i][w] = max(dp[i−1][w], dp[i−1][w−wᵢ] + vᵢ). O(nW). Псевдополиномиален. | Инвестиции в портфель, выбор проектов при ограниченном бюджете, упаковка контейнеров. |
| cs.algorithms.dp.unbounded_knapsack | Unbounded Knapsack | METHOD | Предметы можно брать неограниченно. dp[w] = max(dp[w−wᵢ] + vᵢ). O(nW). | Размен монет, нарезка материала (максимальная стоимость при минимальных отходах). |
| cs.algorithms.dp.lis | Longest Increasing Subsequence | METHOD | ДП O(n²) или бинарный поиск + терпение O(n log n) (алгоритм Фредмана, терпеливая сортировка). | Анализ последовательностей: рейтинги, ДНК, финансовые тренды. |
| cs.algorithms.dp.lcs | Longest Common Subsequence | METHOD | LCS двух строк: dp[i][j] = 1+dp[i−1][j−1] если равны, иначе max(dp[i−1][j], dp[i][j−1]). O(mn). | diff (Git), сравнение ДНК, плагиат, bioinformatics. |
| cs.algorithms.dp.edit_distance | Edit Distance (Levenshtein) | METHOD | Минимальное число вставок/удалений/замен для превращения A в B. dp[i][j] по трём операциям. O(mn). | Автокоррекция, проверка орфографии, нечёткий поиск, сравнение геномов. |
| cs.algorithms.dp.damerau_levenshtein | Damerau-Levenshtein Distance | METHOD | Расширение с транспозицией (swap соседних символов). Основная причина ошибок (~80%) — перестановка соседей. | Более точная автокоррекция. «teh» → «the» — одна транспозиция, а не две замены. |
| cs.algorithms.dp.matrix_chain | Matrix Chain Multiplication | METHOD | Оптимальный порядок умножения цепочки матриц. dp[i][j] = min(dp[i][k] + dp[k+1][j] + pᵢ₋₁·pₖ·pⱼ). O(n³). | Оптимизация вычислений в компьютерной графике, рендеринге, тензорных вычислениях. |
| cs.algorithms.dp.longest_palindromic_substring | Longest Palindromic Substring | METHOD | DP O(n²) или алгоритм Манакера O(n): расширение вокруг центров с использованием уже найденных палиндромов. | Поиск палиндромов в ДНК (сайты рестрикции), анализ текстов. |
| cs.algorithms.dp.coin_change | Coin Change Problem | METHOD | Минимальное число монет для суммы. dp[s] = min(dp[s−cᵢ]) + 1. Или число способов: dp[s] = Σ dp[s−cᵢ]. O(S·n). | Кассовые аппараты, банкоматы (выдача купюрами), оптимальный размен. |
| cs.algorithms.dp.subset_sum | Subset Sum | METHOD | Существует ли подмножество с суммой S? dp[i][s] = dp[i−1][s] OR dp[i−1][s−aᵢ]. O(nS). | Планирование загрузки транспорта, балансировка нагрузки, криптоанализ рюкзачных криптосистем. |
| cs.algorithms.dp.rod_cutting | Rod Cutting | METHOD | Максимальная выручка от разрезания стержня. dp[n] = max(pᵢ + dp[n−i]). O(n²). | Оптимальный раскрой материалов, нарезка труб, оптимизация производства. |
| cs.algorithms.dp.largest_sum_subarray | Kadane's Algorithm | METHOD | Подмассив с максимальной суммой: dp[i] = max(a[i], dp[i−1]+a[i]). O(n). | Анализ финансовых временных рядов, поиск пиков сигнала, обработка изображений. |
| cs.algorithms.dp.maximum_product_subarray | Maximum Product Subarray | METHOD | Подмассив с макс. произведением: два dp (мин и макс из-за минусов). O(n). | Сигналы с негативными значениями, финансы (макс. доход с реинвестированием). |
| cs.algorithms.dp.travelling_salesman_dp | TSP via DP (Held-Karp) | METHOD | DP по подмножествам: dp[mask][i] — мин путь, посещая mask, заканчивая в i. O(n²2ⁿ). | Точное решение TSP для n≤20. Практические задачи с малым числом точек. |
| cs.algorithms.dp.bitmask_dp | DP with Bitmask | METHOD | Состояние — битовая маска (подмножество). Подходит для n≤20. Переход: добавление элемента к маске. | Задача о назначениях, matching, покрытие множества, задача коммивояжёра. |
| cs.algorithms.dp.digit_dp | Digit DP | METHOD | Подсчёт чисел в диапазоне с заданными свойствами через DP по разрядам. Состояние: позиция + флаги. | Сколько чисел без цифры 3 от 1 до N? Сколькими способами? Комбинаторика чисел. |
| cs.algorithms.dp.tree_dp | DP on Trees | METHOD | ДП на деревьях: рекурсивные функции от детей к родителю. Два состояния: берём вершину в решение или нет. | Максимальное независимое множество в дереве, минимальное вершинное покрытие, диаметр. |
| cs.algorithms.dp.convex_hull_trick | Convex Hull Trick | METHOD | Оптимизация DP вида dp[i] = min(k[j]·x[i] + b[j]): линии, convex hull, монотонная очередь. | Ускорение DP с O(n²) до O(n) для класса задач с линейной функцией стоимости. |
| cs.algorithms.dp.divide_and_conquer_dp | Divide & Conquer DP | METHOD | Оптимизация ДП с монотонностью точки разделения (opt[i][j] ≤ opt[i][j+1]). Спуск по дереву рекурсии. | Специфичная, но мощная оптимизация для задач вроде разбиения массива на k групп. |
| cs.algorithms.dp.kadane_2d | Maximum Sum Rectangle 2D | METHOD | Прямоугольник с макс. суммой: фиксируем строки, Kadane по сумме столбцов. O(n²·m). | Обработка изображений, поиск горячих зон в матрицах данных. |
| cs.algorithms.greedy.activity_selection | Activity Selection | METHOD | Выбор максимального числа непересекающихся интервалов: всегда бери заканчивающийся раньше всех. | Планирование встреч, распределение ресурсов (одна аудитория — максимум лекций). |
| cs.algorithms.greedy.huffman_coding | Huffman Coding | METHOD | Оптимальное префиксное кодирование: min-heap, объединение двух наименее частых символов. O(n log n). | ZIP, JPEG, MP3, DEFLATE (gzip, PNG). Сжатие без потерь. |
| cs.algorithms.greedy.fractional_knapsack | Fractional Knapsack | METHOD | Делимые предметы: сортировка по удельной ценности v/w, брать по убыванию пока помещается. O(n log n). | Инвестиции с частичным выкупом, смеси, закупка сырья. |
| cs.algorithms.greedy.huffman_optimality | Huffman Optimality Proof | INVARIANT | Жадный выбор для Хаффмана оптимален: два наименее частых символа — братья в оптимальном дереве. Доказательство заменой. | Обоснование корректности Хаффмана. Классический пример доказательства жадного алгоритма. |
| cs.algorithms.greedy.matroid | Matroid Greedy | INVARIANT | Жадный алгоритм корректен на матроиде. Матроид: наследственность + свойство замены. | Обобщение: остовные деревья (графический матроид), линейная независимость (линейный матроид). |
| cs.algorithms.greedy.exchange_argument | Exchange Argument | METHOD | Стандартный приём доказательства жадных алгоритмов: замена элемента оптимального решения на жадный без ухудшения. | Техника доказательства для interval scheduling, минимального времени ожидания, scheduling. |
| cs.algorithms.string.kmp | Knuth-Morris-Pratt (KMP) | METHOD | Поиск подстроки за O(n+m). Префикс-функция π[i] — длина наибольшего собственного префикса=суффикса. | grep, поиск в текстовых редакторах, поиск вирусных сигнатур, ДНК. |
| cs.algorithms.string.rabin_karp | Rabin-Karp Algorithm | METHOD | Поиск через хеш подстроки. Скользящий хеш: H' = (H − s[i]·p^(L−1))·p + s[i+L]. O(n+m) среднее. | Поиск множества паттернов, плагиат (совпадающие хеши = подозрительный фрагмент). |
| cs.algorithms.string.z_function | Z Algorithm | FUNCTION | Z[i] — длина наибольшего префикса всей строки, совпадающего с подстрокой от i. O(n). | Поиск паттерна (P$T → Z[i] = |P| когда паттерн найден), поиск периода строки. |
| cs.algorithms.string.aho_corasick | Aho-Corasick Automaton | METHOD | Поиск множества паттернов в тексте одним проходом. Trie + суффиксные ссылки + выходные ссылки. O(n+m+matches). | Антивирусы (поиск тысяч сигнатур), фильтрация контента, поиск ключевых слов в спам-фильтрах. |
| cs.algorithms.string.suffix_array | Suffix Array | METHOD | Отсортированные индексы всех суффиксов. Построение O(n log n) (удвоение префиксов) или O(n) (SA-IS, DC3). | Bioinformatics (BWA, Bowtie), сжатие (bzip2), поиск подстрок, longest common prefix. |
| cs.algorithms.string.lcp_array | LCP Array (Kasai) | METHOD | LCP[i] = длина общего префикса SA[i] и SA[i−1]. Алгоритм Касаи за O(n). | Вместе с SA: longest repeated substring, число различных подстрок, longest common substring. |
| cs.algorithms.string.burrows_wheeler | Burrows-Wheeler Transform | METHOD | BWT — перестановка символов строки, кластеризующая повторы. Восстановима через LF-mapping. | bzip2 (сжатие), FM-index (поиск в геномах), bowtie/BWA — read alignment в биоинформатике. |
| cs.algorithms.string.rolling_hash | Polynomial Rolling Hash | METHOD | h(s) = Σ s[i]·p^i mod M. Выбор p~31 (буквы), M — большое простое или 2⁶⁴ (переполнение). Двойной хеш для надёжности. | Rabin-Karp, поиск дубликатов, longest common substring через бинарный поиск + хеши. |
| cs.algorithms.string.manacher | Manacher's Algorithm | METHOD | Палиндромы в строке за O(n): использует симметрию уже найденных палиндромов. Центр + радиус. Расширение только при необходимости. | Поиск палиндромов в ДНК, анализ текстов, криптоанализ. |
| cs.algorithms.string.edit_distance_optimization | Edit Distance Optimizations | METHOD | Hirschberg: O(nm) время, O(min(n,m)) память. Ukkonen cutoff: только полоса ширины k вокруг диагонали. | Огромные строки в ограниченной памяти. Выравнивание геномов. |
| cs.algorithms.string.minimum_window_substring | Minimum Window Substring | METHOD | Наименьшее окно, содержащее все символы шаблона: два указателя + счётчики. O(n). | Поиск сниппетов в поисковиках, анализ текстов, минимальный контекст для ключевых слов. |
| cs.algorithms.string.longest_substring_without_repeating | Longest Substring Without Repeats | METHOD | Два указателя + hash map позиций последнего вхождения. O(n). | Анализ уникальных фрагментов ДНК, поиск максимальной энтропии в тексте. |
| cs.algorithms.string.suffix_automaton | Suffix Automaton | METHOD | Минимальный детерминированный автомат, принимающий все суффиксы строки. O(n) состояний и переходов. | Проверка вхождения, число различных подстрок, longest common substring (две строки). |
| cs.algorithms.string.palindromic_tree | Eertree (Palindromic Tree) | METHOD | Дерево всех различных палиндромов строки + суффиксные ссылки. Построение online O(n). | Подсчёт палиндромов, longest palindromic substring, задачи на палиндромы. |
| cs.algorithms.string.lyndon_factorization | Lyndon Factorization | METHOD | Разложение строки на лексикографически минимальные подстроки (слова Линдона). Алгоритм Дюваля O(n). | Поиск минимального циклического сдвига, каноническая форма строки, биоинформатика. |
| cs.algorithms.string.hamming_distance | Hamming Distance | METHOD | Число позиций с различающимися символами. O(n). Для равных длин. | Коды с коррекцией ошибок, сравнение ДНК, проверка целостности данных. |
| cs.algorithms.string.longest_common_substring | Longest Common Substring | METHOD | Наибольшая общая подстрока (непрерывная): suffix tree/array + LCP, или бинарный поиск + rolling hash. O(n+m). | Сравнение текстов, плагиат, геномные сборки, сравнение версий кода. |
| cs.algorithms.string.shortest_common_supersequence | Shortest Common Supersequence | METHOD | Кратчайшая строка, содержащая A и B как подпоследовательности. DP: scs = |A|+|B|−lcs. | Объединение геномных контигов, сборка ДНК из фрагментов. |
| cs.algorithms.string.regex_matching_dp | Regex Matching via DP | METHOD | Проверка соответствия строки регулярному выражению с * и .: DP по позициям в строке и паттерне. O(sp). | Простейший regex-движок. Реальные — через NFA/DFA (алгоритм Томпсона). |
