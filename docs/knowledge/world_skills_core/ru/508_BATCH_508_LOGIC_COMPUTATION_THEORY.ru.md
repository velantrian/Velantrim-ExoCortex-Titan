# BATCH 508: Logic — Advanced Topics & Theory of Computation

**KnowledgeUnits:** 48  
**Namespace:** `logic.computation.*`  
**Scope:** computability, turing_machines, church_turing_thesis, decidability, complexity_classes, lambda_calculus, type_theory, formal_verification.

| ID | KnowledgeUnit | Тип | Суть | Практический смысл |
|---|---|---|---|---|
| logic.computation.turing_machine | Машина Тьюринга | INVARIANT | Машина Тьюринга — абстрактная модель вычислений: бесконечная лента, головка чтения/записи, конечное число состояний, таблица переходов. | Теоретический фундамент информатики. Тезис Чёрча-Тьюринга: всё вычислимое в интуитивном смысле вычислимо на МТ. |
| logic.computation.church_turing_thesis | Тезис Чёрча-Тьюринга | INVARIANT | Тезис Чёрча-Тьюринга: класс функций, вычислимых алгоритмически, совпадает с классом частично-рекурсивных функций = вычислимых на МТ = λ-определимых. | Не теорема (недоказуема), но эмпирически подтверждена. Все языки программирования эквивалентны по выразительной силе МТ (тьюринг-полнота). |
| logic.computation.halting_problem | Проблема с остановкой | PROCESS | Проблема остановки: не существует алгоритма, определяющего по программе и входу, остановится ли программа. Неразрешима (Тьюринг, 1936). | Фундаментальное ограничение вычислений. Нельзя написать идеальный антивирус, доказывающий безвредность любой программы. |
| logic.computation.decidability_vs_semidecidability | Разрешимость против полуразрешимости | VARIANT | Разрешимая проблема: алгоритм всегда даёт ответ ДА/НЕТ за конечное время. Полуразрешимая: алгоритм говорит ДА если ответ ДА, иначе может не остановиться. | Проблема остановки полуразрешима (запусти и жди), но не разрешима. Верификация программ в общем виде неразрешима. |
| logic.computation.reduction | Снижение | METHOD | Сведение проблемы A к B: решение B даёт решение A. Если A неразрешима, то B тоже. Пишем A ≤ B. | Доказательство неразрешимости: сведение проблемы остановки к своей задаче. |
| logic.computation.rice_theorem | Теорема Райса | INVARIANT | Теорема Райса: любое нетривиальное свойство поведения программы неразрешимо. «Нетривиальное» = не для всех и не ни для одной программы. | Нельзя алгоритмически проверить, завершается ли программа, не содержит ли ошибок, вычисляет ли квадрат — для произвольной программы. |
| logic.computation.recursive_functions | Рекурсивные функции | INVARIANT | Класс частично-рекурсивных функций: базовые (ноль, successor, проекция) + композиция + примитивная рекурсия + минимизация. | Третий эквивалент формализации вычислимости (наряду с МТ и λ-исчислением). |
| logic.computation.primitive_recursive | Примитивные рекурсивные функции | VARIANT | Примитивно-рекурсивные функции: без оператора минимизации. Всегда завершаются. Включают сложение, умножение, exp, но НЕ функцию Аккермана. | Безопасное подмножество: гарантия завершения. Подходит для форматов доказательств с гарантией терминации. |
| logic.computation.ackermann_function | Функция Аккермана | VARIANT | Функция Аккермана A(m,n) — всюду определённая, но НЕ примитивно-рекурсивная. Растёт быстрее любой примитивно-рекурсивной функции. | Пример функции, требующей полной силы общей рекурсии (минимизации). A(4,4) астрономически велико. |
| logic.computation.lambda_calculus | Лямбда-исчисление | INVARIANT | λ-исчисление (Чёрч, 1936): переменная, абстракция λx.M, аппликация MN. β-редукция: (λx.M)N → M[N/x]. | Основа функционального программирования (Lisp, Haskell, ML). Теоретический фундамент через Curry-Howard. |
| logic.computation.church_encoding | Церковное кодирование | METHOD | Кодирование Чёрча: числа, булевы, пары, списки — через λ-термы. 0 = λf.λx.x, 1 = λf.λx.f x, succ = λn.λf.λx.f (n f x). | Показывает, что λ-исчисление тьюринг-полно без встроенных типов данных. Всё — функции. |
| logic.computation.fixed_point_combinator | Y-комбинатор | INVARIANT | Y-комбинатор Y = λf.(λx.f(x x))(λx.f(x x)) — оператор неподвижной точки, позволяет рекурсию в λ-исчислении без именования. | Выразимость рекурсии в чисто функциональном стиле. Необходим для тьюринг-полноты λ-исчисления. |
| logic.computation.complexity_class_p | Класс сложности П | VARIANT | Класс P — задачи, разрешимые детерминированной МТ за полиномиальное время O(n^k) для некоторого k. | Практически вычислимые задачи: сортировка (nlogn), поиск в графе (n+m), умножение матриц (n²·³⁷). |
| logic.computation.complexity_class_np | Класс сложности NP | VARIANT | Класс NP — задачи, решение которых проверяемо за полиномиальное время. Поиск решения может быть экспоненциальным. | Коммивояжёр, SAT, рюкзак, раскраска графа, разложение на множители. P vs NP — открытая проблема. |
| logic.computation.np_completeness | NP-полнота | INVARIANT | NP-полная задача: принадлежит NP и любая задача из NP сводится к ней. SAT — каноническая NP-полная (теорема Кука-Левина, 1971). | Если хоть одну NP-полную задачу решить за полином → P=NP. Практически: эвристики, аппроксимации для NP-трудных задач. |
| logic.computation.np_hard | NP-сложные задачи | VARIANT | NP-трудная задача: к ней сводятся все задачи из NP. Не обязательно лежит в NP (может быть сложнее). Проблема остановки — NP-трудна, но не NP. | Задачи оптимизации: минимальное вершинное покрытие, максимальная клика — NP-трудны, но не в NP (не решение ДА/НЕТ). |
| logic.computation.pspace | PSPACE Сложность | VARIANT | PSPACE — задачи, разрешимые с полиномиальной памятью (время не ограничено). Включает P, NP. Известно: P ⊆ NP ⊆ PSPACE. | Задачи планирования, игры (шахматы n×n), QBF (квантифицированная булева формула) — PSPACE-полны. |
| logic.computation.cook_levin | Теорема Кука-Левина | INVARIANT | Теорема Кука-Левина: SAT (выполнимость булевых формул) NP-полна. Любую NP-задачу можно свести к SAT. | Краеугольный камень теории сложности. Обоснование использования SAT-решателей как универсального инструмента. |
| logic.computation.p_vs_np | Проблема P против NP | INVARIANT | Открытая проблема: P = NP? Одна из семи задач тысячелетия (Clay Institute, $1M). Равны ли классы задач, решаемых и проверяемых за полином? | Если P=NP: криптография на факторизации рухнет, оптимизация станет лёгкой. Большинство учёных считает P≠NP. |
| logic.computation.oracle_machines | Машины Oracle | MODEL | Машина с оракулом — МТ с доступом к «чёрному ящику», мгновенно решающему некую задачу. Иерархия по оракулам. | Изучение относительной сложности. P^A vs NP^A: существуют оракулы, где P=NP и где P≠NP (релятивизация). |
| logic.computation.type_theory | Теория типов | INVARIANT | Теория типов (Рассел, Чёрч, Мартин-Лёф): приписывание типа каждому терму. Тип — спецификация поведения. «a : A» означает «a имеет тип A». | Основа строго типизированных языков. Предотвращение ошибок на уровне компиляции. Формальная математика. |
| logic.computation.simply_typed_lambda | Простое лямбда-исчисление | VARIANT | Просто типизированное λ-исчисление λ→: термы имеют типы. Сильная нормализация — все программы завершаются. Не тьюринг-полно. | Гарантия завершения ценой выразительности. Учебная модель, ядро систем вроде Isabelle/HOL. |
| logic.computation.polymorphism | Параметрический полиморфизм | VARIANT | Полиморфизм: ∀α. α → α — функция, работающая для любого типа. Система F (Жирар, Рейнольдс). тьюринг-полна. | Generics в Java/C#, шаблоны в C++, полиморфные функции в Haskell. Переиспользование кода без потери безопасности. |
| logic.computation.dependent_types | Зависимые типы | INVARIANT | Зависимые типы: Π(x:A). B(x) — тип результата зависит от значения аргумента. Объединяет типы и значения. | Agda, Coq, Idris: доказательство свойств программы в типах. sorted_list — список с гарантией отсортированности. |
| logic.computation.formal_verification | Формальная проверка | METHOD | Формальная верификация — математическое доказательство корректности программы относительно спецификации. Противоположность тестированию. | Критические системы: Airbus A380 (управление полётом), чипы Intel (нет бага FDIV), протоколы безопасности. |
| logic.computation.model_checking | Проверка модели | METHOD | Проверка модели — автоматическая верификация конечных систем: исчерпывающий обход пространства состояний. Эдмунд Кларк, Тьюринговская премия 2007. | Верификация протоколов (TCP/IP, кэш-когерентность), аппаратуры (процессоры), драйверов устройств. |
| logic.computation.temporal_logic_of_actions | Спецификация TLA+ | METHOD | TLA+ (Leslie Lamport) — язык спецификаций на темпоральной логике действий для проектирования распределённых систем. | Amazon (AWS: DynamoDB, S3), Microsoft (Azure CosmosDB) используют TLA+ для предотвращения багов дизайна. |
| logic.computation.abstract_interpretation | Абстрактная интерпретация | METHOD | Абстрактная интерпретация (Кузин-Кузин) — статический анализ через приближение семантики программы абстрактной моделью. | Анализаторы: Astrée (Airbus — отсутствие runtime-ошибок), Infer (Facebook), Polyspace. |
| logic.computation.symbolic_execution | Символическое исполнение | METHOD | Символическое исполнение: вместо конкретных значений — символы. Для каждого пути — ограничения. Решение SMT-солвером. | KLEE, angr: автоматический поиск уязвимостей, генерация тестов, reverse engineering. |
| logic.computation.smt_solver | SMT-решатель | METHOD | SMT-солвер (Satisfiability Modulo Theories) — решает формулы первого порядка с учётом теорий (арифметика, битовые векторы, массивы). | Z3 (Microsoft), CVC4: верификация ПО, синтез программ, поиск эксплойтов, решение судоку. |
| logic.computation.hoare_logic | Логика Хоара | INVARIANT | Логика Хоара: аксиомы и правила вывода для троек {P}C{Q}. Аксиома присваивания: {P[E/x]} x:=E {P}. | Формальное доказательство корректности императивных программ. Фундамент аксиоматической семантики. |
| logic.computation.separation_logic | Логика разделения | INVARIANT | Разделяющая логика (Рейнольдс, О'Хирн): расширение логики Хоара для программ с указателями и кучей. Оператор разделяющей конъюнкции P∗Q. | Доказательство отсутствия утечек памяти, гонок данных. Инструменты: VeriFast, Infer, Smallfoot. |
| logic.computation.linear_logic | Линейная логика | INVARIANT | Линейная логика (Жирар, 1987): ресурсы используются ровно один раз. Операторы ⊗ (одновременно), & (выбор), ! (неограниченно). | Управление ресурсами, типы сессий для параллельных программ, квантовые вычисления. |
| logic.computation.bisimulation | Бисимуляция | INVARIANT | Бисимуляция — отношение эквивалентности между переходами двух систем. Если A симулирует B и B симулирует A, они эквивалентны. | Доказательство корректности оптимизаций компилятора: исходная и оптимизированная программа должны быть бисимулярны. |
| logic.computation.process_calculus | Процесс исчисления | MODEL | Исчисления процессов: CCS (Милнер), π-исчисление, CSP (Хоар). Формальные модели параллелизма и коммуникации. | Основа теории параллельных систем, верификация протоколов. Erlang, Go — практические наследники CSP. |
| logic.computation.deadlock_freedom | Тупик Свободы | CONTROL | Свобода от дедлоков: в системе не существует состояния, где каждый процесс ждёт другого. Типизация сессий гарантирует deadlock-freedom. | Банковские системы, телеком, базы данных. Дедлок = остановка системы. |
| logic.computation.liveness_property | Живая недвижимость | INVARIANT | Свойство живости: «что-то хорошее в конце концов произойдёт». G(request → F response). Отсутствие голодания (starvation). | Гарантия прогресса: лифт когда-нибудь приедет, запрос когда-нибудь выполнится, светофор переключится. |
| logic.computation.safety_property | Безопасность собственности | INVARIANT | Свойство безопасности: «ничего плохого никогда не случится». G(¬bad). Инварианты системы. | Лифт не поедет с открытыми дверями, поезд не проедет на красный, счёт не уйдёт в минус без авторизации. |
| logic.computation.refinement | Уточнение | METHOD | Уточнение (refinement): конкретная имплементация сохраняет все наблюдаемые свойства абстрактной спецификации. A ⊑ C. | Пошаговая разработка: от спецификации → дизайн → код. Каждый шаг уточняет, не нарушая спецификацию. |
| logic.computation.rust_borrow_checker | Логика владения и заимствования | METHOD | Логика владения и заимствования: каждое значение имеет ровно одного владельца; ссылки либо уникальные мутабельные, либо множественные иммутабельные (не оба). | Rust: безопасность памяти без сборщика мусора. Предотвращение use-after-free, data race на уровне компиляции. |
| logic.computation.total_functional_programming | Полное функциональное программирование | METHOD | Тотальное функциональное программирование: все функции тотальны (завершаются). Требует structural recursion, корекурсии, productivity. | Завершаемость по построению. Без bottom (⊥). Epigram, Idris, Agda. Типы гарантируют терминацию. |
| logic.computation.homotopy_type_theory | Теория гомотопических типов (HoTT) | MODEL | Гомотопическая теория типов: типы — пространства, термы — точки, равенства — пути. Аксиома унивалентности Воеводского. | Новый фундамент математики. Формализация в Coq. Эквивалентность = равенство. Вычислительная интерпретация высшей теории категорий. |
| logic.computation.automated_theorem_proving | Автоматизированное доказательство теорем (ATP) | METHOD | Автоматическое доказательство теорем: E, Vampire, Zipperposition (FOL), HOLyHammer, Sledgehammer. Соревнование CASC. | Помощь в формальной верификации: рутинные шаги автоматизируются, сложные — интерактивно. |
| logic.computation.interactive_theorem_proving | Интерактивное доказательство теорем (ITP) | METHOD | Интерактивное доказательство: человек направляет, машина проверяет каждый шаг. Coq, Isabelle/HOL, Lean. | Формализация математики (4-цветная теорема, теорема Фейта-Томпсона, perfectoid spaces в Lean). |
| logic.computation.curry_howard_lambek | Переписка Карри-Ховарда-Ламбека | INVARIANT | Расширение Карри-Ховарда на категории (Ламбек): доказательства = морфизмы в декартово-замкнутой категории. Триединство: логика, вычисления, категории. | Глубинная связь трёх дисциплин. Категорная семантика языков программирования. |
| logic.computation.kolmogorov_complexity | Колмогоровская сложность | INVARIANT | Колмогоровская сложность K(x) — длина кратчайшей программы, выводящей x. Инвариантна относительно выбора универсальной МТ (с точностью до константы). | Мера информации. Случайность = несжимаемость. Связь с энтропией Шеннона. Нижняя граница сжатия данных. |
| logic.computation.chaitin_incompleteness | Незавершенность Чайтина | INVARIANT | Теорема Чайтина: существует константа L, такая что никакая формальная система не может доказать K(x) > L ни для какой строки x. | Новое доказательство неполноты через теорию информации. Нельзя доказать, что строка «случайна» сверх определённого предела. |
| logic.computation.algorithmic_information | Алгоритмическая теория информации | INVARIANT | Алгоритмическая теория информации (Колмогоров, Соломонов, Чайтин): соединение вычислений и информации. Априорная вероятность Соломонова M(x) = 2^{-K(x)}. | Универсальное индуктивное предсказание (Solomonoff induction). Теоретический оптимум машинного обучения (невычислим). |
