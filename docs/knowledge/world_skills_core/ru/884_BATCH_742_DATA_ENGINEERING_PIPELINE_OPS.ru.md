# BATCH 742: Data Engineering — Pipeline & Infrastructure

**KnowledgeUnits:** 18
**Namespace:** `dataeng.ops.*`
**Scope:** ETL_ELT, data_warehouse, lakehouse, streaming, Spark, dbt, data_quality, orchestration, schema_evolution, CDC

| ID | KnowledgeUnit | Тип | Суть | Практический смысл |
|---|---|---|---|---|
| dataeng.ops.etl_vs_elt | ETL против ELT — шаблоны архитектуры | METHOD | ETL (Extract, Transform, Load): преобразование до загрузки в хранилище. ELT (Extract, Load, Transform): загрузка необработанных данных → трансформация в хранилище (SQL или Spark). Преимущества ELT: облачные DW (Snowflake, BigQuery) = дешёвое хранилище + дорогие вычисления по требованию. Архитектура медальона: Бронза (необработанная) → Серебро (очищенное) → Золото (агрегированное). | ELT + dbt: современный стандарт для analytical pipelines. |
| dataeng.ops.spark_rdd_dataframe | Apache Spark — RDD против DataFrame | METHOD | Apache Spark: RDD (Resilient Distributed Dataset): иммутабельная распределённая коллекция. DataFrame: структурированные данные (оптимизатор Catalyst). Набор данных: типизированный DataFrame (Scala). Ленивая оценка: только при действии (собрать, записать, посчитать). Широкие преобразования (join, groupBy): перемешивание между разделами. Узкий (карта, фильтр): без перемешивания. | Catalyst query optimizer: автоматическая оптимизация → 10-100× быстрее ручного RDD. |
| dataeng.ops.kafka_consumer_group | Кафка — Группы потребителей | METHOD | Apache Kafka: Производитель → Тема → Потребитель. Раздел: единица параллелизма. Группа потребителей: каждый раздел → только один потребитель. Смещение: позиция в разделе. Auto.offset.reset: самый ранний или последний. Коэффициент репликации: 3 (производство). ISR (синхронные реплики). Семантика «по крайней мере один раз», «не более одного раза» и «точно один раз». Реестр схемы (Avro/Protobuf). | Кафка-лаг: отставание потребителя от производителя → Оповещение при лаге > порог. |
| dataeng.ops.dbt_transformation | dbt — инструмент построения данных | METHOD | dbt (инструмент построения данных): трансформации в SQL с версионированием. Модели: операторы SELECT (материализованные в виде таблицы/представления/инкрементного/снимка). Постановка → Средний → Март. Тесты: уникальные, not_null, принятые_значения, отношения. Документация: YAML-описание. Происхождение: граф зависимостей. dbt Cloud против ядра. Конвейер CI/CD для преобразований SQL. | Поэтапная материализация: только новые данные + стратегия MERGE = эффективность. |
| dataeng.ops.data_quality_great_expectations | Качество данных — большие надежды | METHOD | Большие надежды: Python-библиотека для тестирования качества данных. Ожидания: ожидания = тесты на данные (столбец должен быть не нулевым, значения должны быть между X-Y). Люкс «Ожидание». Контрольные точки: запуск тестов в конвейере. Документы с данными: HTML отчёт о качестве. Профиль: ожидания создания собственных данных. Интеграция: Airflow, Spark, dbt. | Контракт данных: явное соглашение между производителем и потребителем о схеме/качестве. |
| dataeng.ops.snowflake_architecture | Snowflake — многокластерная архитектура | METHOD | Snowflake: разделение storage (S3/Azure Blob/шкала комы Глазго) и compute. Virtual Warehouses: независимые compute clusters. Автоскейлинг. Time Travel: запрос исторических данных (до 90 дней). Zero-Copy Cloning: мгновенная клонировка без дублирования данных. Snowpipe: автоматическая загрузка при появлении файлов в S3. Failsafe: 7 дней после Time Travel. | Снежинка Кредит:блок компьютера. XS = 1 кредит/ч, 3XL = 128 кредит/ч. |
| dataeng.ops.lakehouse_delta_lake | Озеро Дельта — транзакции ACID | METHOD | Delta Lake (Databricks): ACID-транзакция на уровне хранилища объектов (S3, ADLS). Дельта-журнал: JSON-файлы с изменениями в истории. Уплотнение (ОПТИМИЗАЦИЯ): объединение мелких файлов. ZORDER: кластеризация по часто фильтруемым полям. Путешествие во времени: ВЕРСИЯ НА СОСТОЯНИЕ, ОТМЕТКА ВРЕМЕНИ НА СОСТОЯНИЕ. Эволюция схемы: MERGE INTO. Фид данных изменений (CDF). | Таблица Дельта: решает «проблему с маленькими файлами» в Parquet/Spark. |
| dataeng.ops.airflow_dag | Воздушный поток — структура DAG | METHOD | Apache Airflow: DAG (направленные ациклические графики) = рабочие процессы. Операторы: PythonOperator, BashOperator, SparkSubmitOperator, SQLExecuteQueryOperator. Зависимости задач: >>. XCom: обмен данными между задачами. Планировщик + Веб-сервер + Исполнитель (LocalExecutor, CeleryExecutor, KubernetesExecutor). SLA (Соглашение об уровне обслуживания): оповещения об опоздании задач. | Airflow не = инструмент обработки данных (только оркестровка). |
| dataeng.ops.schema_evolution | Эволюция схемы — стратегии | METHOD | Схемы эволюции: добавление новых колонок (обратная совместимость). Удаление колонок: критическое изменение (воздействие по течению). Переименование: взлом. Тип изменения: взлом. Стратегии: схема при чтении (Avro), схема при записи (Parquet). Реестр схем Avro: управление совместимостью. Режимы совместимости НАЗАД, ВПЕРЕД, ПОЛНАЯ. | Schema drift: неожиданное изменение данных от источника → автоматическое обнаружение важно. |
| dataeng.ops.cdc_change_data_capture | CDC — сбор измененных данных | METHOD | CDC (Change Data Capture): отслеживание изменений в исходной базе данных → потоковые обновления. Методы: CDC на основе журналов (Debezium: читает WAL PostgreSQL, binlog MySQL — самый надёжный). На основе триггера. На основе временных меток (опрос — не в реальном времени). Дебезий → Кафка → Флинк/Искра → цель. Вариант использования: синхронизация данных в реальном времени, источник событий. | CDC на основе журналов: не влияет на производительность исходной базы данных. |
| dataeng.ops.data_warehouse_modeling | Хранилище данных — Кимбалл против Инмона | METHOD | Кимбалл (снизу вверх): многомерное моделирование — таблицы фактов + таблицы измерений. Схема звезды. OLAP-запросы через денормализованную структуру. Соответствующие размеры. Автобусная архитектура. Инмон (сверху вниз): фабрика корпоративной информации. Нормализованная предметная область 3НФ. ETL → Хранилище данных → Витрины данных. Комбинация: Хранилище + схема звезды. | One Big Table (OBT): плоская широкая таблица = простейшая, но повторы и накладные расходы на память. |
| dataeng.ops.partitioning_strategy | Разделение данных — стратегии | METHOD | Разделение данных: Статическое разбиение: дата, регион, категория. Динамическое секционирование: по ключу из данных. Разделение куста: каталоги в HDFS/S3 (дата=2024-01-01/). Очистка разделов: механизм SQL читает только нужные партии. Чрезмерное разбиение: слишком много маленьких файлов → накладные расходы на метаданные. Ведро (хэш-разделение): для равномерного распределения при соединениях. | Выбор ключа раздела: наиболее часто используемый фильтр = ключ (дата, регион). |
| dataeng.ops.data_contracts | Контракты данных — стандарты | METHOD | Дата-контракты: явное соглашение между производителями и потребителями данных. Компоненты: схема (поля, типы), соглашения об уровне обслуживания (свежесть, доступность), семантика (определения), право собственности, ожидания в отношении качества. OpenDataContract (ODC): открытый стандарт. Бизнес = производитель + потребитель = данные — как API (модель SaaS для данных). Data Mesh: владение доменом + данные как продукт + платформа самообслуживания. | Без контрактов: молчаливые критические изменения → последующие сбои без предупреждения. |
| dataeng.ops.batch_vs_streaming_latency | Пакетная обработка или потоковая передача — компромиссы | METHOD | Пакетная обработка (пакетная обработка): высокая пропускная способность, задержка (высокие минуты). Микропакетная передача (структурированная потоковая передача Spark): женщины 1–5 мин. Потоковая передача (Flink, Kafka Streams): задержка менее секунды. Lambda Architecture: пакетные + потоковые слои. Kappa Architecture: только потоковая передача. Обработка сложных событий (CEP): шаблоны в потоке событий. | Требование к задержке: определите архитектуру. Почти в реальном времени (<1 мин): микропакет. В реальном времени (<100 мс): потоковая передача. |
| dataeng.ops.row_vs_columnar_storage | Формат хранения — Parquet или CSV | METHOD | Форматы хранения данных: CSV: текстовый, все типы как strings, нет сжатия, нет scheme. Parquet: columnar — эффективен для аналитических запросов (читает только нужные колонки). Snappy/ZSTD compression. ORC: похожий (Hive ecosystem). Avro: row-based + schema evolution (streaming). Delta/Iceberg/Hudi: table formats поверх Parquet. | Увеличение скорости в 10 раз: Parquet и CSV для аналитических запросов (обрезка столбцов). |
| dataeng.ops.orchestration_comparison | Оркестровка — Airflow против Prefect против Dagster | METHOD | Инструменты оркестрации: Airflow: доминирует, на основе DAG, Python, сложная настройка. Prefect: более Pythonic, лучше для динамических рабочих процессов, Prefect Cloud. Dagster: ориентирован на активы (активы данных = первоклассный), сильная линия передачи данных, система типов. Временной: механизм рабочего процесса для длительных бизнес-процессов. Mage: современная альтернатива Airflow. | На основе активов и на основе задач: Dagster = активы (что производишь), Airflow = задачи (что делаешь). |
| dataeng.ops.star_schema_design | Схема «Звезда» — разработка таблицы фактов | METHOD | Звёздная схема: Таблица фактов (транзакции, событий): измеримые метрики (количество, сумма). Внешние ключи к измерениям. Аддитивные, полуаддитивные и неаддитивные факты. Медленно изменяющиеся измерения (SCD): тип 1 (перезапись), тип 2 (новая строка с версией), тип 3 (новый столбец). Вырожденное измерение: таблица фактов (order_number). | SCD Type 2: самый распространённый для исторического анализа (customer address changes). |
| dataeng.ops.data_lineage_tools | Данные Lineage — OpenLineage | METHOD | Прослеживаемость данных (Data Lineage): откуда данные, куда идут, через какие преобразования. OpenLineage: открытый стандарт метаданных. Marquez: open-source lineage server. Apache Atlas, DataHub, Collibra: enterprise tools. Column-level lineage: детальнее чем table-level. Impact analysis: что сломается при изменении upstream? | Регуляторное требование (GDPR): знать где персональные данные хранятся и используются. |
| dataeng.ops.flink_stateful | Apache Flink — потоковая передача с отслеживанием состояния | METHOD | Flink: распределенная потоковая обработка. Состояние: состояние ключа (для каждого ключа), состояние оператора. Контрольные точки: асинхронное сохранение состояния (для восстановления). Ровно один раз: через двухфазную фиксацию + контрольные точки Flink. Время события и время обработки: время события + водяные знаки (обработка поздних данных). Типы окон: переворачивающиеся, раздвижные, сеансовые. | Backpressure: downstream медленнее upstream → Flink автоматически снижает скорость. |
