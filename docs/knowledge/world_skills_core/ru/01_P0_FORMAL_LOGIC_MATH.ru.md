# 🧮 P0 Formal Core — математика, логика, статистика, вычисления

**Язык:** русский  
**Статус:** seed pack v0.1 / не L3 truth  
**Цель:** собрать компактные единицы знания, которые дают Velantrim формальный скелет мышления: считать, выводить, проверять, различать строгий вывод и вероятностное рассуждение.

---

## Правило слоя

Математика и классическая логика могут быть почти `ImmutableCore`, но только после нормализации терминов и проверки формализации.

```text
строгие правила → Validated / ImmutableCore
вероятностные правила → Supported / Validated
эвристики → Supported
философские трактовки логики → Hypothesized / Supported
```

---

## 📦 Knowledge Units

**KnowledgeUnits:** 53

| ID | Тип | Утверждение / суть | Условия и границы | Связи |
|---|---|---|---|---|
| `math.set.element` | СРОК | Множество задаётся элементами; принадлежность записывается как `x ∈ A`. | Требуется выбранная теория множеств. | логика.предикат |
| `math.function.mapping` | СРОК | Функция сопоставляет каждому элементу области определения ровно одно значение. | Многозначные соответствия не являются функциями без уточнения. | math.relation |
| `math.equality.substitution` | ПРАВИЛО | Если `a = b`, то `a` можно заменить на `b` в корректном выражении. | В пределах одной формальной системы. | логика.идентичность |
| `math.order.transitive` | ПРАВИЛО | Транзитивность порядка: из `a ≤ b` и `b ≤ c` следует `a ≤ c`. | Для транзитивных отношений порядка. | логика.транзитивность |
| `math.arithmetic.associative_add` | ЗАКОН | `(a + b) + c = a + (b + c)`. | Для стандартных числовых структур, где сложение ассоциативно. | алгебра |
| `math.arithmetic.distributive` | ЗАКОН | `a(b + c) = ab + ac`. | В кольцах, полях и стандартной арифметике. | алгебра |
| `math.number.prime` | СРОК | Простое число имеет ровно два положительных делителя: 1 и само себя. | Обычно для натуральных чисел больше 1. | делимость |
| `math.divisibility.gcd` | СРОК | НОД двух чисел — наибольшее число, делящее оба без остатка. | Для целых чисел. | евклид_алгоритм |
| `math.linear_equation` | МЕТОД | Уравнение `ax + b = 0` при `a ≠ 0` имеет решение `x = -b/a`. | Не работает при `a = 0` без разбора случая. | алгебра |
| `math.quadratic_formula` | МЕТОД | Для `ax²+bx+c=0`, `a≠0`, корни: `x=(-b±√(b²-4ac))/(2a)`. | Над вещественными числами число корней зависит от дискриминанта. | алгебра |
| `math.pythagorean_theorem` | ТЕОРЕМА | В прямоугольном треугольнике `a² + b² = c²`. | Только евклидова геометрия, прямой угол. | геометрия, инженерия.обследование |
| `math.trigonometry.sine` | СРОК | `sin(θ)` в прямоугольном треугольнике = противолежащий катет / гипотенуза. | Базовое определение для прямоугольного треугольника; расширяется через окружность. | география.навигация |
| `math.trigonometry.cosine` | СРОК | `cos(θ)` = прилежащий катет / гипотенуза. | См. ограничения синуса. | навигация, физика.вектор |
| `math.trigonometry.tangent` | СРОК | `tan(θ)=sin(θ)/cos(θ)`. | Не определена при `cos(θ)=0`. | склон, road_design |
| `math.geometry.area_circle` | ФОРМУЛА | Площадь круга `A = πr²`. | Евклидова геометрия. | инженерные.трубы |
| `math.geometry.volume_cylinder` | ФОРМУЛА | Объём цилиндра `V = πr²h`. | Идеализированная форма. | резервуары, трубы |
| `math.calculus.derivative` | СРОК | Производная показывает мгновенную скорость изменения функции. | Требуется предел; не все функции дифференцируемы. | физика.скорость |
| `math.calculus.integral` | СРОК | Интеграл может представлять накопленную величину или площадь под графиком. | Требуется выбранный тип интеграла и условия интегрируемости. | работа, вероятность |
| `math.vector.dot_product` | ФОРМУЛА | Скалярное произведение `a·b = | a |  | b | cosθ`. | В евклидовом пространстве. | physics.work |
| `math.matrix.linear_system` | МЕТОД | Линейные системы можно записывать как `Ax=b`. | Решение зависит от ранга и обратимости матрицы. | инженерное моделирование. |
| `logic.identity` | ЗАКОН | Закон тождества: `A` есть `A`. | Классическая логика. | математика.равенство |
| `logic.non_contradiction` | ЗАКОН | `A` и `¬A` не могут быть истинны одновременно в одном смысле и контексте. | Классическая логика; параконсистентные логики допускают иное обращение с противоречиями. | ворота истины |
| `logic.excluded_middle` | ЗАКОН | Закон исключённого третьего: `A ∨ ¬A`. | Классическая логика; в интуиционистской логике ограничено. | доказательство |
| `logic.modus_ponens` | ИНФЕРЕНС_ПРАВИЛО | Modus ponens (прямой вывод): из `A → B` и `A` следует `B`. | Дедуктивный вывод. | рассуждение.дедуктивное |
| `logic.modus_tollens` | ИНФЕРЕНС_ПРАВИЛО | Если `A → B` и `¬B`, то `¬A`. | Дедуктивный вывод. | фальсификация |
| `logic.hypothetical_syllogism` | ИНФЕРЕНС_ПРАВИЛО | Гипотетический силлогизм: из `A→B` и `B→C` следует `A→C`. | Цепочка импликаций. | причинная_цепочка |
| `logic.disjunctive_syllogism` | ИНФЕРЕНС_ПРАВИЛО | Дизъюнктивный силлогизм: из `A∨B` и `¬A` следует `B`. | Классическая логика. | решение |
| `logic.de_morgan.1` | ЗАКОН | Закон де Моргана (отрицание конъюнкции): `¬(A ∧ B) = ¬A ∨ ¬B`. | Булева логика. | схемы |
| `logic.de_morgan.2` | ЗАКОН | Закон де Моргана (отрицание дизъюнкции): `¬(A ∨ B) = ¬A ∧ ¬B`. | Булева логика. | схемы |
| `logic.contraposition` | ИНФЕРЕНС_ПРАВИЛО | `A→B` эквивалентно `¬B→¬A` в классической логике. | Не все логики принимают без ограничений. | доказательство |
| `logic.universal_instantiation` | ИНФЕРЕНС_ПРАВИЛО | Из `∀x P(x)` следует `P(a)` для конкретного `a`. | При корректной области дискурса. | predicate_logic |
| `logic.existential_introduction` | ИНФЕРЕНС_ПРАВИЛО | Из `P(a)` следует `∃x P(x)`. | При наличии объекта `a` в области. | predicate_logic |
| `logic.proof_by_contradiction` | МЕТОД | Чтобы доказать `A`, допускают `¬A` и выводят противоречие. | Классическая логика; осторожно в конструктивных системах. | математическое доказательство |
| `logic.induction_math` | МЕТОД | Если база верна и шаг `n→n+1` верен, утверждение верно для всех натуральных `n`. | Для утверждений над натуральными числами. | математика.естественная |
| `logic.induction_empirical` | МЕТОД | Обобщение из наблюдений даёт вероятный, но не строгий вывод. | Может быть опровергнуто новым наблюдением. | наука |
| `logic.abduction` | МЕТОД | Абдукция выбирает лучшее объяснение наблюдения. | Не гарантирует истинность; требует проверки. | диагноз |
| `logic.bayesian_update` | МЕТОД | Новые данные обновляют вероятность гипотезы через prior, likelihood и posterior. | Требуются вероятностная модель и допущения. | статистика |
| `logic.causal_intervention` | МЕТОД | Причинный вопрос спрашивает не только "связано ли", а "что будет, если вмешаться". | Требуется causal model; корреляции недостаточно. | world_model |
| `logic.modal.necessity` | ЛОГИК_ТИП | Модальная логика различает необходимость и возможность. | Значение зависит от типа модальности. | философия |
| `logic.temporal` | ЛОГИК_ТИП | Временная логика описывает, что было, есть, будет или всегда/иногда истинно. | Важна для систем, событий и верификации программ. | би_временной |
| `logic.deontic` | ЛОГИК_ТИП | Деонтическая логика описывает обязанное, разрешённое и запрещённое. | Нормативная логика; не равна фактической истинности. | этика, безопасность |
| `logic.epistemic` | ЛОГИК_ТИП | Эпистемическая логика описывает знание и убеждения агентов. | Не заменяет проверку источников. | след, агент |
| `logic.paraconsistent` | ЛОГИК_ТИП | Параконсистентная логика позволяет работать с противоречиями без полного логического взрыва. | Полезна для конфликтующих баз знаний; требует маркировки противоречий. | inconsistency_hunter |
| `stats.mean` | ФОРМУЛА | Среднее арифметическое = сумма значений / число значений. | Чувствительно к выбросам. | data_anaанализ |
| `stats.median` | МЕТОД | Медиана делит упорядоченный набор пополам. | Устойчива к выбросам. | Robust_stats |
| `stats.variance` | ФОРМУЛА | Дисперсия измеряет средний квадрат отклонения от среднего. | Единицы измерения возводятся в квадрат. | неопределенность |
| `stats.correlation_not_causation` | ОГРАНИЧЕНИЕ | Корреляция не доказывает причинность. | Возможны скрытые переменные и обратная причинность. | причинная_модель |
| `stats.conditional_probability` | ФОРМУЛА | Условная вероятность: `P(A | B)=P(A∧B)/P(B)` при `P(B)>0`. | Требуется корректная вероятность события B. | bayes |
| `info.entropy` | ФОРМУЛА | Энтропия Шеннона измеряет неопределённость распределения. | Не равна физической энтропии без уточнения. | сжатие |
| `cs.algorithm` | СРОК | Алгоритм — конечная процедура решения класса задач. | Должны быть определены вход, шаги и завершение. | вычисление |
| `cs.complexity.big_o` | МЕТОД | Big-O описывает асимптотический рост затрат алгоритма. | Скрывает константы и реальные аппаратные эффекты. | производительность |
| `cs.database.index` | МЕХАНИЗМ | Индекс ускоряет поиск ценой дополнительной памяти и стоимости обновления. | Неподходящий индекс может не помочь запросу. | поиск |
| `cs.hash.sha256` | МЕТОД | SHA-256 даёт криптографический дайджест фиксированной длины. | Хэш не шифрует данные и не доказывает истинность содержимого. | аудит_цепочка |
