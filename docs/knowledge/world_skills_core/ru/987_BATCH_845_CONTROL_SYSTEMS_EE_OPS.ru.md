# BATCH 845: Electrical Engineering — Control Systems

**KnowledgeUnits:** 45
**Namespace:** `controlsys.ops.*`
**Scope:** transfer_functions, PID_tuning, state_space, stability, frequency_response, digital_control, robust_control, feedback_control

| ID | KnowledgeUnit | Тип | Суть | Практический смысл |
|---|---|---|---|---|
| controlsys.ops.transfer_function | Передаточная функция — область Лапласа | METHOD | Передаточная функция: G(s) = Y(s)/U(s) в Лапласовской области. Полюса (знаменателя нули): стабильность. Нули (числителя нули): отклик. Единичная обратная связь: Closed-loop T(s) = G(s)/(1+G(s)H(s)). Характеристическое уравнение: 1+G(s)H(s) = 0. DC gain: G(0). | Полюса в левой полуплоскости: стабильная система. В правой: нестабильная. |
| controlsys.ops.root_locus | Корневой локус — Дизайн | METHOD | Метод корневого годографа: как корни перемещаются при изменении K. Правила: начало у полюсов G(s)H(s), конец у нулей. Угол отправления/прихода. Точки пересечения Real axis. Центроид асимптот: (Σpolюса - Σнули)/(n-m). Проектирование: выбор K для желаемых полюсов. | Root Locus: инструмент для настройки коэффициента усиления. |
| controlsys.ops.bode_plot | Частотная характеристика — график Боде | METHOD | Диаграмма Боде: Отношение амплитуды (дБ) в зависимости от ω (логарифм). Фаза в зависимости от ω (логарифм). Усиление Частота разделения ωgc: | G(jω) | = 0 dB. Phase Crossover ωpc: ∠G(jω) = -180°. Gain margin: 1/ | G(jωpc) | dB. Phase margin: 180° + ∠G(jωgc). GM > 6 dB + PM > 45°: хорошая устойчивость. | Большой PM: медленный отклик. Малый PM: быстрый но нестабильный. |
| controlsys.ops.nyquist_stability | Найквист — критерий устойчивости | METHOD | Критерий Найквиста: Z = N + P. Z = правополосные нули замкнутой. N = обходы (-1, 0) по часовой. P = правополосные полюса разомкнутой. Z = 0 для стабильности. Contour: охватывает правую полуплоскость. Упрощение: если P=0, то N=0 для стабильности. | Найквист vs Боде: Найквист точнее для нестандартных систем. |
| controlsys.ops.pid_ziegler_nichols | ПИД — настройка Циглера-Николса | METHOD | Настройка по Циглеру-Николсу: Метод реакции (Step response): K = slope/ultimate gain. Метод предельного усиления: усилить K до незатухающих осмсколебаний → Ku, Tu. PID: Kp = 0.6 Ku, Ti = Tu/2, Td = Tu/8. Ограничения: консервативный для некоторых процессов. | Cohen-Coon: улучшенный метод для первого порядка + чистая задержка. |
| controlsys.ops.state_space | Государственное пространство — представление | METHOD | Пространство состояний: ẋ = Ax + Bu, y = Cx + Du. A: матрица динамики. B: матрица входа. C: матрица выхода. D: прямая связь. Наблюдаемость + управляемость: условия для проектирования. Решение: x(t) = e^At x(0) + ... Eigenvalues(A) = полюса. | Размещение полюсов (формула Аккермана): выбор желаемых собственных значений → находим К. |
| controlsys.ops.observer_design | Наблюдатель — Люенбергер | METHOD | Наблюдатель Луенбергера: оценка состояния из измерений. ẋ̂ = Aх̂ + Bu + L(y - Cх̂). L = gain matrix. Eigenvalues наблюдателя: быстрее чем полюса регулятора (~3-5×). Separation principle: регулятор и наблюдатель проектируются независимо. Kalman filter: оптимальный наблюдатель при шуме. | Фильтр Калмана: СИНИЙ (лучший линейный несмещенный оценщик). |
| controlsys.ops.digital_control_sampling | Цифровое управление — отбор проб | METHOD | Цифровое управление: Теорема дискретизации Шеннона: fs ≥ 2f_max. ZOH (Zero-Order Hold): удерживает между выборками. Tustin (bilinear) transform: s → 2/T × (z-1)/(z+1). Z-domain. Stability: все полюса внутри единичного круга. Anti-aliasing filter: перед АЦП. | Практическая выборка: fs = 5-20× полоса пропускания (не только 2×). |
| controlsys.ops.cascade_control | Каскадное управление — внутренний-внешний цикл | METHOD | Каскадное управление: внутренний (fast) + внешний (slow) контроллер. Inner: быстрее (отклонения подавляются раньше). Outer: управляет setpoint для inner. Применение: температура реактора (внешний) + расход охладителя (внутренний). | Правило: Inner loop должен быть в 5-10× быстрее outer loop. |
| controlsys.ops.feedforward_control | Упреждающая связь — подавление помех | METHOD | Опережающее регулирование (Feedforward): компенсирует до нарушения достигнет процесса. FF = -Gd(s)/Gp(s). Не требует обратной связи с отклонением. Идеальный FF: полная компенсация (нереально — неточные модели). Комбинирование: FF + FB = наилучший. | FF быстро, FB надёжно: комбинация = optimal. |
| controlsys.ops.anti_windup | Интегратор Windup — Антивиндап | METHOD | Интегратор Windup: при насыщении актуатора → integrator accumulates → при снятии ограничения → пересыщение. Anti-windup methods: conditional integration (остановить при sat), back-calculation. Conditional integration: стоп интегрирования при насыщении + ошибка правильного знака. | Без anti-windup: большие отклонения при снятии ограничений (windup response). |
| controlsys.ops.model_predictive | MPC — управление с прогнозированием модели | METHOD | МРС (Model Predictive Control): оптимизация на горизонте предсказания N. Ограничения: входные + выходные. Online optimization (QP). Receding horizon. Industrial use: нефтехимия, HVAC, автономные авто. Computational load: обновлено каждый цикл. | MPC обрабатывает ограничения явно (в отличие от PID неявно). |
| controlsys.ops.frequency_domain_robustness | Робастное управление — H∞ | METHOD | Робастное управление (H∞): минимизация H∞ норма = максимальное сингулярное значение. Границы неопределенности. Функция чувствительности S(s). Дополнительная чувствительность T(s). Проблема смешанной чувствительности. Теорема о малом выигрыше. MATLAB Robust Control Toolbox. | H∞ control: гарантированная стабильность при bounded uncertainty. |
| controlsys.ops.nonlinear_control | Нелинейные системы — Ляпунов | METHOD | Нелинейные системы: Метод Ляпунова: найти V(x) > 0 такое, что dV/dt < 0 → стабильность. Квадратичная форма: V = xᵀPx. SDRE (зависимое от состояния уравнение Риккати): нелинейный LQR. Линеаризация: расширение Тейлора в состоянии равновесия. | Lyapunov: глобальная стабильность если V радиально несвязана. |
| controlsys.ops.optimal_control_lqr | LQR — линейный квадратичный регулятор | METHOD | Линейный квадратичный регулятор (LQR): минимизировать J = ∫(xᵀQx + uᵀRu)dt. Q: вес состояний. R: вес управлений. Optimal gain K = R⁻¹BᵀP, где P решает Riccati equation. Trade-off Q vs R: больший Q → агрессивный, больший R → экономный. | LQR: системы без ограничений. MPC: с ограничениями. |
| controlsys.ops.delay_systems | Временная задержка — Предсказатель Смита | METHOD | Системы с чистым запаздыванием (dead time): τd — задержка транспортирования. Smith Predictor: внутренняя модель без задержки + компенсация задержки. G(s)e^(-τs). Delay margin = Phase margin / ω×τ. | Большие системы с мертвым временем: θ/τ > 0,5 → труден для обычного ПИД. |
| controlsys.ops.lead_lag_compensation | Lead-Lag компенсация — коррекция фазы | METHOD | Lead-компенсатор: C(s) = K × (T₁s+1)/(αT₁s+1), α < 1 — добавляет фазовое опережение (до 60°). Lag-компенсатор: C(s) = K × (T₂s+1)/(βT₂s+1), β > 1 — повышает низкочастотное усиление. Lead улучшает PM и быстродействие; Lag снижает установившуюся ошибку. Комбинированная Lead-Lag цепь применяется последовательно. | Lead = больше PM и bandwidth. Lag = меньше steady-state error, но медленнее. |
| controlsys.ops.h2_control | H₂ оптимальное управление — минимизация энергии | METHOD | H₂-норма: ∥G∥₂ = √(1/2π ∫₋∞^∞ trace(G*(jω)G(jω)) dω). Оптимальный H₂ регулятор минимизирует энергию выхода при белом шуме на входе — обобщение LQR/LQG. Решение через Riccati уравнения. Применяется для систем с известными спектрами возмущений. | H₂ оптимален для стохастических возмущений; LQG — частный случай H₂. |
| controlsys.ops.sliding_mode | Скользящий режим — робастное управление | METHOD | Управление со скользящим режимом (SMC): переключающая функция s(x) = 0. Закон управления u = -k·sign(s(x)). Условие скольжения: s·ṡ < 0. Chatter (дребезг) подавляется boundary layer (sat вместо sign). Invariant к matched uncertainties. Теория Филиппова для разрывных систем. | SMC гарантирует робастность при параметрических возмущениях — chatter остаётся главным недостатком. |
| controlsys.ops.adaptive_control | Адаптивное управление — MRAC | METHOD | Модельно-адаптивное управление (MRAC): эталонная модель → выход реальной системы подстраивается под неё. Механизм адаптации: MIT rule или Lyapunov-based. Direct MRAC: параметры регулятора настраиваются online. Indirect: оцениваются параметры объекта, затем пересчитывается регулятор. | MRAC полезен для систем с дрейфующими параметрами (аэрокосмос, робототехника). |
| controlsys.ops.fuzzy_control | Нечёткое управление — FIS | METHOD | Fuzzy Logic Control: fuzzification → rule base (если-то) → inference → defuzzification. Типы Мамдани (интуитивный) и Такаги-Сугено-Канга (аналитический). Функции принадлежности: треугольные, трапецеидальные, гауссовы. Правила: IF error is NB AND derror is ZE THEN output is NS. | Fuzzy control полезен, когда нет точной математической модели, но есть экспертные знания. |
| controlsys.ops.nn_control | Нейросетевое управление — аппроксимация | METHOD | Нейросетевое управление: MLP и RNN для аппроксимации нелинейных динамик. Inverse dynamics control: сеть учит обратную модель. Model predictive NN: нейросеть как модель для MPC. Обучение: offline (имитация) + online (адаптация). Функция активации: ReLU, tanh, sigmoid. | Нейросети эффективны для highly nonlinear dynamics где LQR/SMC недостаточны. |
| controlsys.ops.system_identification | Идентификация систем — ARX/ARMAX | METHOD | Идентификация: построение модели по входо-выходным данным. ARX: y(k) + a₁y(k-1)+... = b₁u(k-1)+... + e(k). ARMAX: + скользящее среднее шума. Subspace methods (N4SID): для MIMO. PRBS (Pseudo-Random Binary Sequence) — стандартный тестовый сигнал. | Для идентификации нужна постоянная возбудимость (persistently exciting) входного сигнала. |
| controlsys.ops.gain_scheduling | Gain Scheduling — параметрическая настройка | METHOD | Gain Scheduling: набор линеаризованных моделей + LQR/PID контроллеров для разных рабочих точек. Параметр расписания (scheduling variable): измеряемый сигнал (скорость, высота, температура). Interpolation: между точками переключения плавная интерполяция коэффициентов. Проблема: нет гарантии устойчивости между точками. | Gain Scheduling широко применяется в авиации (разные высоты и скорости полёта). |
| controlsys.ops.lpv_systems | LPV системы — линейные с параметрами | METHOD | Linear Parameter-Varying (LPV): ẋ = A(θ(t))x + B(θ(t))u, где θ(t) — изменяемый параметр. LPV контроллер: K(θ) синтезируется через Linear Matrix Inequalities (LMI). Self-scheduled control: контроллер автоматически подстраивается по θ. Разница с Gain Scheduling: строгие гарантии устойчивости для LPV. | LPV — математически строгий, но вычислительно более сложный аналог Gain Scheduling. |
| controlsys.ops.iterative_learning | Итеративное обучение — ILC | METHOD | Iterative Learning Control (ILC): для повторяющихся операций — коррекция управления на основе ошибки предыдущей итерации. u_{k+1}(t) = u_k(t) + L·e_k(t+1). Типы: P-type, D-type,高阶 ILC. Применение: роботизированные манипуляторы, химические реакторы batch, 3D-печать. | ILC — zero-error tracking для повторяющихся траекторий после нескольких итераций. |
| controlsys.ops.internal_model_control | IMC — внутренняя модель | METHOD | Internal Model Control (IMC): контроллер содержит модель объекта Q(s), параллельно с реальным процессом. Q(s) ≈ Gₚ⁻¹(s)·f(s), где f(s) — фильтр низких частот. IMC-структура эквивалентна классической обратной связи: C(s) = Q/(1 - Q·G_m). Zero steady-state offset если модель точна. | IMC даёт интуитивный метод настройки PID через выбор фильтра λ. |
| controlsys.ops.deadbeat_control | Deadbeat — минимальное время | METHOD | Deadbeat control (цифровой): выход достигает заданного значения за минимальное число тактов без перерегулирования. Полюса размещаются в нуле z-плоскости. Реакция: | y(k) - r | = 0 для k > n. Недостатки: очень большие управляющие сигналы в начале, чувствительность к неточности модели. | Deadbeat быстр, но требует высокой точности модели и мощных актуаторов. |
| controlsys.ops.minimum_phase | Минимально-фазовые системы — особенности | METHOD | Минимально-фазовые системы: все нули и полюса в левой полуплоскости. Неминимально-фазовые: нули в правой полуплоскости → обратный выброс (undershoot). Нули ППП ограничивают achievable bandwidth. Bode's gain-phase relationship: для МФ-систем фаза однозначно определяется амплитудой. | Неминимально-фазовые нули делают управление принципиально сложнее — нужны предиктивные методы. |
| controlsys.ops.sensitivity_functions | Функции чувствительности — S и T | METHOD | Функция чувствительности S(s) = 1/(1+GK). Дополнительная T(s) = GK/(1+GK). S + T = 1. Малый S → хорошее подавление возмущений. Малый T → робастность к мультипликативной неопределённости. Waterbed effect: снижение | S | в одном диапазоне → рост в другом. | Компромисс S/T — фундаментальное ограничение обратной связи (Bode's sensitivity integral). |
| controlsys.ops.mimo_systems | MIMO системы — частотные характеристики | METHOD | MIMO системы: матричная передаточная функция G(s). Сингулярные значения σ̄(G) и σ̲(G) — обобщение gain. Relative Gain Array (RGA): RGA = G × (G⁻¹)ᵀ — мера взаимовлияния каналов. Decoupling: идеальный (диагонализация) и статический. Condition number: κ = σ̄/σ̲ — мера ill-conditioning. | RGA > 5 на диагонали → сильное взаимодействие, нужна декомпозиция. |
| controlsys.ops.routh_hurwitz | Критерий Рауса-Гурвица — устойчивость | METHOD | Критерий Рауса-Гурвица: по коэффициентам характеристического полинома без вычисления корней. Таблица Рауса: первая строка — чётные коэффициенты, вторая — нечётные. Система устойчива если все элементы первого столбца > 0. Особые случаи: нулевая строка (чисто мнимые корни) и нулевой элемент (метод ε). | Routh-Hurwitz — быстрый analytical метод проверки устойчивости вручную. |
| controlsys.ops.lyapunov_equation | Уравнение Ляпунова — анализ устойчивости | METHOD | Уравнение Ляпунова: AᵀP + PA = -Q. Если для Q > 0 существует P > 0 — система асимптотически устойчива. Дискретный аналог: AᵀPA - P = -Q. Lyapunov function: V(x) = xᵀPx, dV/dt = -xᵀQx ≤ 0. Применяется также для H₂ нормы: ∥G∥₂² = trace(BᵀPB). | Lyapunov equation — основа анализа устойчивости и H₂ оптимального управления. |
| controlsys.ops.kalman_decomposition | Декомпозиция Калмана — структура | METHOD | Калмановская декомпозиция: разделение системы на 4 подпространства: управляемые/наблюдаемые, управляемые/ненаблюдаемые, неуправляемые/наблюдаемые, неуправляемые/ненаблюдаемые. Преобразование подобия: A' = T⁻¹AT. Минимальная реализация: только управляемая+наблюдаемая часть. | Pole placement возможен только для управляемой+наблюдаемой подсистемы. |
| controlsys.ops.balanced_truncation | Балансная редукция — понижение порядка | METHOD | Балансная усечённая редукция: преобразование к балансной реализации, где грамианы управляемости P и наблюдаемости Q равны Σ = diag(σ₁,...σ_n). Hankel singular values: σᵢ = √(λᵢ(PQ)). Отбрасывание состояний с малыми σᵢ. Гарантированная ошибка: ∥G - Gᵣ∥∞ ≤ 2∑σ_{k+1}ⁿσᵢ. | Балансная редукция — стандартный метод уменьшения порядка модели с гарантированной точностью. |
| controlsys.ops.hankel_norm | Норма Ганкеля — энергия прошлого→будущего | METHOD | Норма Ганкеля: ∥G∥H = σ̄₁ — наибольшее сингулярное число Ганкеля. Показывает, какая энергия выхода в будущем (t > 0) может быть вызвана входом в прошлом (t < 0). Оптимальная редукция по Hankel norm: ∥G - Gᵣ∥∞ ≤ ∥G - G_opt∥∞ + ∥G_opt - Gᵣ∥∞. | Hankel norm — точная мера for model reduction quality. |
| controlsys.ops.coprime_factorization | Копримная факторизация — устойчивые множители | METHOD | Копримная (взаимно-простая) факторизация: G = NM⁻¹, где N, M ∈ RH∞. Left coprime: G = M̃⁻¹Ñ. Взаимно-простая факторизация — основа H∞ синтеза. Gap metric: δ(G₁,G₂) — мера разницы между системами, основанная на копримных факторах. | Coprime factorization — математический фундамент для робастного управления H∞. |
| controlsys.ops.youla_parameterization | Параметризация Юлы — все стабилизирующие контроллеры | METHOD | Параметризация Youla-Kučera: все стабилизирующие контроллеры для G = NM⁻¹: K = (X + MQ)(Y - NQ)⁻¹, где Q ∈ RH∞ — свободный параметр. X, Y — решение уравнения Безу: XN + YM = I. Q tuning: оптимизация производительности без потери стабильности. | Youla parameterization — полный класс стабилизирующих контроллеров через один свободный параметр Q. |
| controlsys.ops.internal_model_principle | Принцип внутренней модели — асимптотическое слежение | METHOD | Принцип внутренней модели (IMP): для нулевой установившейся ошибки модель возмущения/задания должна быть включена в контур — контроллер содержит Internal Model. Sinusoidal reference → генератор синуса в контроллере. Step → интегратор. Регулятор с IMP автоматически подавляет гармоники. | IMP — почему интегратор в PID устраняет step-ошибку: модель step-сигнала = 1/s. |
| controlsys.ops.servo_problem | Серво-задача — отслеживание задающего сигнала | METHOD | Серво-задача (servo): выход y(t) → r(t) при изменяющемся задании. 2-DOF структура: feedforward (F) для r + feedback (C) для стабильности. Feedforward: ū = G⁻¹(0)·r для DC. Prefilter: фильтрация задания для уменьшения overshoot. | 2-DOF структура позволяет одновременно хорошее слежение и робастность. |
| controlsys.ops.regulation_problem | Задача регулирования — подавление возмущений | METHOD | Задача регулирования (regulation): поддержание y(t) ≈ 0 при действии возмущений d(t). Peak disturbance rejection: минимизация ∥S∥∞. Типы d: step (нужен интегратор), sinusoidal (нужна Internal Model), random (стохастический — LQG). | Интегратор в регуляторе обеспечивает асимптотическое подавление step-возмущений. |
| controlsys.ops.phases_margin_practical | Запасы устойчивости — практические рекомендации | PRACTICAL | Практические рекомендации по запасам: Phase margin > 30° (минимально), > 45° (хорошо), > 60° (отлично). Gain margin > 6 дБ. Delay margin > 0.1/ωgc. Для интегральных процессов PM ≥ 60°. Для систем с задержкой GM ≥ 10 дБ. | Хорошая практика: PM 45-60° + GM 6-12 дБ + DM > период дискретизации. |
| controlsys.ops.anti_alias_filtering | Фильтрация анти-алиасинга — практика | PRACTICAL | Anti-alias filter перед АЦП: аналоговый ФНЧ 2-4 порядка с частотой среза f_cut ≤ 0.5·f_s. Butterworth: максимально плоская полоса. Bessel: линейная фаза (лучше для временной области). Фильтр устанавливается перед каждой АЦП входом. Порядок фильтра: trade-off между подавлением и фазовым сдвигом. | Без anti-alias фильтра высокочастотный шум маскируется в полосу управления (aliasing). |
| controlsys.ops.quantization_effects | Эффекты квантования — битность АЦП/ЦАП | PRACTICAL | Квантование: resolution = V_ref / 2^N. 12-бит АЦП с V_ref=10В → шаг 2.44 мВ. Quantization noise: SNR ≈ 6.02N + 1.76 дБ. Limit cycles (циклы в установившемся режиме при малом входе). Dithering: добавление малого шума для разбиения limit cycles. | 16-бит АЦП обычно достаточно для управления. Dithering улучшает разрешение при малых сигналах. |
| controlsys.ops.digital_implementation | Цифровая реализация — выбор периода квантования | PRACTICAL | Выбор периода дискретизации T_s: T_s = 1/(10-30)·ω_BW для регуляторов. T_s = 1/(5-10)·ω_BW для наблюдателей. Критерий Найквиста: T_s ≤ π/ω_max. Практически: T_s ≤ 0.1·τ_min (наименьшая постоянная времени). Для промышленных ПЛК: T_s = 0.1-1 с. | T_s слишком мал → избыточные вычисления и шум. T_s слишком велик → потеря управляемости. |
