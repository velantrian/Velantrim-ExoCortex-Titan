# BATCH 727: Data Science — Machine Learning Operations

**KnowledgeUnits:** 20
**Namespace:** `mlops.ops.*`
**Scope:** feature_engineering, model_selection, cross_validation, hyperparameter, deployment, MLflow, drift_detection, A/B_testing

| ID | KnowledgeUnit | Тип | Суть | Практический смысл |
|---|---|---|---|---|
| mlops.ops.feature_engineering | Функциональное проектирование — трансформация | METHOD | Трансформация признаков: Нормализация (Мин-Макс масштабирование: [0,1]). Стандартизация (StandardScaler: µ=0, σ=1). Логарифмическое преобразование: при скошенных распределениях. Бокс-Кокс: оптимальная λ для нормализации. Биннинг: числовые → категориальные. Особенности взаимодействия: А×Б. Полиномиальные характеристики. Вменение отсутствующего значения: среднее/медиана/режим/KNN. | Feature leakage: информация из будущего в обучающих данных → завышенная performance. |
| mlops.ops.bias_variance_tradeoff | Смещение-дисперсия — компромисс | METHOD | Ошибка обобщения = Смещение² + Дисперсия + Неустранимая ошибка. Высокая предвзятость: неадекватность (простая модель). Высокая дисперсия: переобучение (сложная модель). Регуляризация: Ridge (L2, затухание веса), Lasso (L1, выбор признаков). ElasticNet: оба. Отсев (NN): отличается ансамбль. Ансамблевые методы: снижают дисперсию. | Кривые обучения: ошибка обучения, ошибка проверки в зависимости от размера обучения = смещение/дисперсия диагностики. |
| mlops.ops.cross_validation_strategies | Перекрестная проверка — стратегии | METHOD | Кросс-валидация: K-Fold (k=5 или 10): делит данные на k частей, ротация. Stratified K-Fold: сохраняет class distribution. Leave-One-Out (LOO): крайний случай. Time Series: TimeSeriesSplit (нельзя допускать data leakage). Nested CV: внешний для оценки, внутренний для tuning. GroupKFold: исключить одну группу (пациент, пользователь). | Кросс-валидация на тест-сете = утечка данных (делает оценку недействительной). |
| mlops.ops.hyperparameter_tuning | Настройка гиперпараметров — стратегии | METHOD | Подбор гиперпараметров: Grid Search: полный перебор. Random Search (более эффективен). Bayesian Optimization (Optuna, Hyperopt): суррогатная модель предсказывает лучшие параметры. Successive Halving: прерывать плохих кандидатов рано. Population Based Training (Deepmind): параллельный + evolutionary. BOHB (Bayesian Optimization + Hyperband). | Случайный поиск по сравнению с сеткой: на 60–80 % лучше в то же время (Бергстра и Бенджио). |
| mlops.ops.mlflow_experiment_tracking | MLflow — отслеживание экспериментов | METHOD | MLflow: платформа с открытым исходным кодом для жизненного цикла машинного обучения. Отслеживание: параметры журнала, метрики, артефакты. Проекты: воспроизводимые запуски. Модели: реестр моделей (постановка/производство). Обслуживание моделей: REST API, Docker. URI отслеживания: сервер MLflow. run_id: уникальный идентификатор. Автолог: sklearn, TensorFlow, PyTorch автоматически. | Эксперимент без tracking: «что было параметром при лучшем результате?» — не вспомнить. |
| mlops.ops.model_deployment_patterns | Развертывание модели — шаблоны | METHOD | Паттерны деплоя ML-моделей: Batch prediction: периодические, bulk, не real-time. Online prediction: REST API, low-latency. Shadow deployment: production трафик проходит через оба (без последствий от новой модели). Canary: постепенное переключение (5% → 20% → 100%). Blue-Green: мгновенное переключение между версиями. A/B test: явное сравнение двух версий. | Feature store: централизованное хранение признаков (Feast, Tecton). |
| mlops.ops.data_drift_detection | Дрейф данных — методы обнаружения | METHOD | Дрейф данных (datadrift): сдвиг распределения → модель деградирует. Ковариатный сдвиг: p(X) изменилось. Сдвиг метки: p(Y) изменилось. Смещение концепции: p(Y | Х) изменилось. Методы: индекс стабильности населения (PSI), KS-тест, максимальное отклонение среднего значения (MMD), ОЧЕВИДНО AI. Триггеры: переобучение, когда PSI >0,2 или производительность падает. | PSI 0-0.1: нет изменений. 0.1-0.2: умеренный. >0.2: значительный дрейф. |
| mlops.ops.class_imbalance | Дисбаланс классов — Техники | METHOD | Дисбаланс классов: Передискретизация: SMOTE (Техника передискретизации синтетического меньшинства) — создает синтетические образцы меньшего класса. Случайная недостаточная выборка: удалить больше. Веса классов: наказывают ошибки класса меньшинства. Настройка порога: не 0,5 порога. Ансамбль: BalancedBagingClassifier. Метрики: Precision-Recall AUC (вместо ROC AUC при дисбалансе). | Accuracy paradox: 95% accuracy при 95% класс A → модель может просто всегда предсказывать A. |
| mlops.ops.shap_model_explanation | SHAP — Объясняемость модели | METHOD | SHAP (SHapley Additive exPlanations): unified framework для feature importances. Shapley values из теории игр. SHAP values: вклад каждого признака в предсказание. Summary plot: глобальная важность. Force plot: индивидуальное предсказание. Dependence plot: SHAP vs feature value. TreeSHAP: эффективно для деревьев. DeepSHAP: для нейронных сетей. | LIME (Локальные интерпретируемые модельно-агностические объяснения): альтернатива (аппроксимация локально). |
| mlops.ops.neural_architecture_search | Нейронная архитектура — трансферное обучение | METHOD | Transfer Learning: предобученные веса на большом датасете → дообучение на малом. Fine-tuning: разморозить последние слои. Feature extraction: заморозить всё, только classifier обучается. ImageNet weights (ResNet, EfficientNet, ViT): для vision. BERT/GPT: для NLP. Few-shot learning: обучение из малого числа примеров. Zero-shot: без примеров (LLM prompting). | Предобученные модели: экономят недели обучения. |
| mlops.ops.recommender_system | Рекомендательные системы — типы | METHOD | Рекомендательные системы: Совместная фильтрация: на основе поведения похожих пользователей (на основе пользователей, на основе элементов). Матричная факторизация (SVD, ALS): разложение пользовательских матриц. На основе контента: на основе атрибутов элемента и пользователя. Гибридный. Проблема с холодным запуском: новый пользователь/элемент. Оценка: NDCG, Precision@K, MRR. | Popularity bias: рекомендуются популярные → diversity снижается. |
| mlops.ops.causal_inference_ml | Причинно-следственный вывод — методы машинного обучения | METHOD | Каузальный вывод в ML: корреляция ≠ причинно-следственная связь. Структура потенциальных результатов (Рубин): ​​Y(1), Y(0). Средний эффект лечения (ATE). Инструментальные переменные (IV). Разница в различиях (DiD). Разрыв регрессии (RD). DoWhy: библиотека Python для каузального вывода. Конфаундеры: приводят к ложным корреляциям. | A/B-тест = рандомизированный эксперимент → причинно-следственная связь. Наблюдение = корреляция (искажающие факторы). |
| mlops.ops.time_series_forecasting | Временные ряды — модели прогнозирования | METHOD | Прогнозирование временных рядов: ARIMA: авторегрессионное интегрированное скользящее среднее (p,d,q). САРИМА: сезонное. Пророк (Мета): праздничные эффекты, точки изменения тренда. LightGBM/XGBoost с задержками. LSTM для нелинейного. N-BEATS (чистый DL). Оценка: MAPE, RMSE, MAE, sMAPE, MASE. Бэктестирование: расширение или прокручивание окна. | Избежать: тест на тренировочных данных после parameter tuning (look-ahead bias). |
| mlops.ops.fairness_ml | Справедливость машинного обучения — метрики | METHOD | Справедливость в ML: Демографический паритет: P(ŷ=1 | А=0) = Р(ŷ=1 | A=1) (равный positives rate). Equalized Odds: TPR + FPR равны по группам. Individual Fairness: похожие люди → похожие предсказания. Trade-offs: нельзя одновременно выполнить все критерии (Arrow's impossibility). Аудит моделей: Aequitas, Fairlearn, IBM AI Fairness 360. | Proxy variables: даже без protected attribute → могут кодировать race (zip code → race in USA). |
| mlops.ops.federated_learning | Федеративное обучение — конфиденциальность | METHOD | Федеративное обучение (FL): обучение на децентрализованных данных (не собирать в единое место). FedAvg: локальное обучение на узлах → усреднение весов. Differential Privacy: добавить noise для privacy protection (ε-differential privacy). Secure Aggregation: криптографическое агрегирование (никто не видит индивидуальные градиенты). Challenges: non-IID data (heterogeneous devices). | Google Gboard: FL для text prediction на Android без отправки текстов на сервер. |
| mlops.ops.gpt_prompting | LLM Подсказки - Инжиниринг | METHOD | Оперативная инженерия: нулевой выстрел: просто задача без примеров. Несколько кадров (контекстное обучение): 2–5 в примерах. Цепочка мыслей (ЦТ): «Давайте думать шаг за шагом» → уточним рассуждения. ReAct: Рассуждение + Действие (инструменты). Самосогласованность: сэмплировать несколько CoT → голосование. Дерево мыслей. RAG (Поисковая дополненная генерация): документы включаются в контекст. | Prompt injection: вредоносный ввод перезаписывает инструкции. |
| mlops.ops.graph_neural_networks | Графовые нейронные сети — типы | METHOD | Графовые нейронные сети (GNN): нейронная сеть передачи сообщений (MPNN). GCN (сверточная сеть графов). GAT (Graph Attention Network): вес рёбер. GraphSAGE: индуктивный (новые узлы без переобучения). Применение: молекулярные свойства, рекомендации, обнаружение мошенничества, графы знаний. Задачи уровня узла/границы/графа. PyG (PyTorch Geometric), DGL. | Гетерогенные графы: разные типы узлов и рёбер (HAN, R-GCN). |
| mlops.ops.anomaly_detection_types | Обнаружение аномалий — методы | METHOD | Обнаружение аномалий: Точечная аномалия: Единственное отклонение. Контекстуальный: аномально в десятилетие (тепература лета). Коллектив: группа аномалий. Методы: изоляционный лес, LOF (локальный коэффициент выбросов), DBSCAN (шумовые точки), автоэнкодер (высокая ошибка реконструкции). Временной ряд: реконструкция LSTM, декомпозиция STL. Порог: фиксированный или адаптивный. | Неконтролируемое обнаружение аномалий: нет меток (типично для реального мира). |
| mlops.ops.model_compression | Сжатие модели — методы | METHOD | Сжатие модели: Pruning (обрезка): удалить малозначимые веса (unstructured vs structured). Quantization: FP32 → INT8 (в 4× меньше, быстрее). Knowledge Distillation: маленькая student модель учится у большой teacher. Low-rank factorization: разложить матрицы весов. ONNX: формат для cross-platform deployment. TensorRT: NVIDIA оптимизация для inference. | Обучение с учетом квантования (QAT): лучшее качество, чем квантование после обучения (PTQ). |
| mlops.ops.evaluation_metrics_classification | Метрики оценки — Классификация | METHOD | Метрики классификации: Точность = (TP+TN)/(TP+TN+FP+FN). Точность = TP/(TP+FP). Напомним (Чувствительность) = TP/(TP+FN). F1 = 2×(P×R)/(P+R). ROC-AUC: без порога. Матрица путаницы: полное изображение. Коэффициент корреляции Мэтьюза (MCC): для несбалансированного. Калибровка: шкала Брайера, диаграмма надежности. | F1-macro: среднее по классам (равный вес). F1-weighted: взвешенное по поддержке. |
