# BATCH 764: Software Engineering — Design Patterns & Architecture

**KnowledgeUnits:** 20
**Namespace:** `softeng.ops.*`
**Scope:** SOLID_principles, design_patterns, microservices, system_design, REST_API, testing_strategies, performance, database_design, security_patterns

| ID | KnowledgeUnit | Тип | Суть | Практический смысл |
|---|---|---|---|---|
| softeng.ops.solid_principles | ТВЕРДЫЙ — Принципы | METHOD | SOLID принципы: Single Responsibility (один класс = одна ответственность). Open/Closed (открыт для расширения, закрыт для изменения). Liskov Substitution (подтипы должны заменять базовые). Interface Segregation (не заставлять реализовывать ненужные методы). Dependency Inversion (зависеть от абстракций, не конкретики). | Нарушение SOLID: сильная связь → изменения каскадные по всей системе. |
| softeng.ops.factory_pattern | Шаблоны проектирования — Фабрика | METHOD | Factory Method: определить интерфейс создания объекта, но позволить подклассам решить какой класс создавать. Abstract Factory: семейство связанных объектов без указания конкретных классов. Builder: конструирование сложного объекта шаг за шагом. Prototype: клонировать существующие объекты. Singleton: один экземпляр + глобальная точка доступа. | Singleton: анти-паттерн в многопоточных (нужна синхронизация) + трудно тестировать. |
| softeng.ops.strategy_observer_pattern | Поведенческие модели — наблюдатель | METHOD | Observer (Publish-Subscribe): subject уведомляет observers при изменении состояния. Strategy: семейство алгоритмов + взаимозаменяемость. Command: инкапсуляция запроса как объекта. Template Method: скелет алгоритма в суперклассе. Chain of Responsibility: цепочка handlers. Iterator: последовательный доступ без раскрытия внутреннего представления. | Архитектура, управляемая событиями: шаблон наблюдателя в масштабе. |
| softeng.ops.microservices_decomposition | Микросервисы — Декомпозиция | METHOD | Декомпозиция на микросервисы: по домену (DDD — Domain Driven Design, Bounded Contexts). По бизнес-возможностям. Принципы: Единая ответственность, автономность, собственные данные (нет общей базы данных), API прежде всего. Связь: синхронизация (REST/gRPC) и асинхронность (событие/очередь). Service Mesh (Istio, Linkerd): наблюдаемость + управление трафиком. | Распределенный монолит: антипаттерн — микросервисы, но с жесткой связью. |
| softeng.ops.rest_api_design | REST API — принципы проектирования | METHOD | RESTful API: Ресурсы: URI = существительное (/users/{id}). Методы HTTP: GET (извлечение), POST (создание), PUT (замена), PATCH (частичное обновление), DELETE. Коды состояния: 200 (ОК), 201 (создан), 400 (неверный запрос), 401 (несанкционированный), 403 (запрещено), 404 (не найден), 500 (ошибка сервера). Управление версиями: /v1/users. НЕНАВИСТЬ. | Идемпотент: ПОЛУЧИТЬ, ПОСТАВИТЬ, УДАЛИТЬ. Неидемпотентный: POST. |
| softeng.ops.cap_theorem | Теорема CAP — Распределенные системы | METHOD | CAP теорема (Brewer): в распределённой системе при сетевом разделении (Network Partition) нельзя одновременно иметь Consistency и Availability. CP: согласованность (MongoDB, HBase). AP: доступность (CouchDB, Cassandra). CA: без партиций (одиночный сервер). PACELC: расширение CAP (добавляет latency vs consistency trade-off). | Real-world: всегда есть сетевые сбои → выбор CP vs AP. |
| softeng.ops.database_normalization | База данных — формы нормализации | METHOD | Нормализация: 1NF: атомарные значения (нет множественных значений в ячейке). 2NF: нет частичных зависимостей (все неключевые атрибуты зависят от ВСЕГО первичного ключа). 3NF: нет транзитивных зависимостей. BCNF: каждый детерминант = суперключ. 4NF: нет многозначных зависимостей. | Денормализация: для read-performance (аналитические запросы). |
| softeng.ops.testing_pyramid | Тестирование пирамиды — стратегии | METHOD | Пирамида тестирования: Unit tests (основание): быстрые, изолированные. Integration tests (средний слой): несколько компонентов. E2E tests (вершина): всё приложение. AAA pattern (Arrange-Act-Assert). TDD (Test-Driven Development): сначала тест. Mocking: фиктивные зависимости. Coverage: 80%+ unit. | Антипаттерн: Рожок мороженого (много E2E, мало единиц). |
| softeng.ops.event_driven_saga | Управляемый событиями — шаблон Saga | METHOD | Шаблон Saga: для распределенных транзакций без двухфазной фиксации. По принципу хореографии: каждый сервис публикует мероприятие и подписывается на другие. На основе оркестровки: центральный оркестратор. Компенсирующие операции: откат при день. Источник событий: храните события как источник истины. CQRS (разделение ответственности за запросы команд): разные модели чтения и записи. | Distributed transactions: нет единственного правильного решения (Saga vs 2PC trade-offs). |
| softeng.ops.caching_strategies | Кэширование — стратегии | METHOD | Стратегии кэширования: Cache-aside (Lazy loading): приложение управляет кэшем (read-through на miss). Write-through: при записи обновлять кэш + DB. Write-behind: кэш обновляется, DB асинхронно. Refresh-ahead: предварительное обновление до истечения TTL. LRU/LFU eviction policies. Redis, Memcached. | Cache invalidation: одна из сложнейших проблем в программировании. |
| softeng.ops.container_kubernetes | Kubernetes — основные концепции | METHOD | Kubernetes (K8s): Pod = наименьшая единица (один или несколько контейнеров). Развертывание: желаемое состояние Pods. Сервис: сетевая абстракция. Вход: HTTP-маршрутизация. ConfigMap/Secret: изменение. HPA (Horizontal Pod Autoscaler): автомасштабирование. Пространство имен: логическое разделение. РБАК: доступ. Helm: менеджер пакетов для K8s. | Pod → Привязка узла, антипривязка: размещение по правилам. |
| softeng.ops.hexagonal_architecture | Шестиугольная архитектура — порты и адаптеры | METHOD | Гексагональная архитектура (Cockburn 2005): бизнес-логика в центре. Ports: интерфейсы. Adapters: конкретные реализации (REST, DB, message queue). Dependency Rule: зависимости направлены внутрь (к домену). Clean Architecture (Uncle Bob): Entities → Use Cases → Interface Adapters → Frameworks. | Луковая архитектура: аналогичная концепция (слои направлены внутрь). |
| softeng.ops.domain_driven_design | DDD — Тактические схемы | METHOD | Domain-Driven Design (Evans 2003): Entity: идентифицируется по ID. Value Object: идентифицируется по атрибутам. Aggregate: кластер entities (Aggregate Root). Repository: доступ к агрегатам. Domain Service: логика не принадлежащая entity. Domain Event: что случилось. Bounded Context: ограниченная область применения модели. | Вездесущий язык: единый язык бизнеса + код. |
| softeng.ops.circuit_breaker | Устойчивость — автоматический выключатель | METHOD | Circuit Breaker: предотвращает каскадные сбои. States: Closed (нормальная работа), Open (все запросы провалены без попытки), Half-Open (проверочный запрос). Thresholds: ошибки > X% за интервал → Open. Recovery: автоматически после timeout → Half-Open → Closed при успехе. Libraries: Resilience4j, Hystrix (deprecated). | Bulkhead pattern: изолировать ресурсы (thread pools) для разных сервисов. |
| softeng.ops.oauth2_jwt | Аутентификация — OAuth 2.0 и JWT | METHOD | OAuth 2.0: сервер авторизации (AuthZ), сервер ресурсов, клиент. Потоки: код авторизации + PKCE (веб-приложения), учетные данные клиента (между компьютерами), код устройства (IoT). JWT (веб-токен JSON): header.payload.signature (в кодировке Base64). Претензии: sub, iss, aud, exp, iat. Проверка подписи (алгоритм RS256/HS256). | Access Token vs Refresh Token: короткоживущий (15 мин) + долгоживущий (renew). |
| softeng.ops.event_loop_node | Цикл событий — Node.js | METHOD | Цикл событий (Node.js): один поток + неблокирующий ввод-вывод. Фазы: таймеры (setTimeout) → обратные вызовы ввода-вывода → простой → опрос (ожидание ввода-вывода) → проверка (setImmediate) → закрытие. Очередь микрозадач: обратные вызовы обещаний = выполняются после каждой фазы. Рабочие потоки: для ресурсоемких задач. Противодавление: медленный потребитель < быстрый производитель. | libuv: библиотека, реализующая Event Loop в Node.js. |
| softeng.ops.database_index_types | Индексирование базы данных — типы | METHOD | Типы индексов: B-Tree: универсальный (диапазон + равенство). Хэш: только равенство (поиск O(1)). Индекс покрытия: все запрошенные столбцы в индексе (нет доступа к таблице). Частичный индекс: подмножество данных. GiST (PostgreSQL): геопространственные. Составной индекс: порядок колонок критический (крайнее левое префиксное правило). | EXPLAIN ANALYZE: посмотреть план запроса перед оптимизацией. |
| softeng.ops.message_queues | Очереди сообщений — RabbitMQ против Kafka | METHOD | Очереди сообщений: RabbitMQ: традиционный брокер сообщений на основе push, гибкая маршрутизация, низкая задержка (<1 мс). Kafka: основанный на извлечении данных, распределенный журнал, высокая пропускная способность (миллионы в секунду), постоянный (воспроизводится). RabbitMQ: очереди работы/задачи. Kafka: источник событий, потоковая обработка. AMQP (RabbitMQ) против собственного протокола Kafka. | DLQ (очередь недоставленных писем): неудавшиеся сообщения для анализа и повторной попытки. |
| softeng.ops.load_balancing_algorithms | Балансировка нагрузки — алгоритмы | METHOD | Алгоритмы балансировки нагрузки: Round Robin: по очереди. Взвешенный круговой турнир: серверы с разной ёмкостью. Наименьшее количество связей: к серверу с наименьшим числом активных. IP-хэш: закрепленные сеансы (тот же клиент → тот же сервер). Случайный. Проверка работоспособности: отключить неработоспособное состояние. Балансировка нагрузки уровня 4 (TCP) и уровня 7 (HTTP/HTTPS). | Балансировщик нагрузки уровня 7: может маршрутизировать URL-адрес (/api → серверная часть). |
| softeng.ops.api_gateway | API-шлюз — функции | METHOD | API-шлюз: единая точка входа для микросервисов. Функции: маршрутизация, ограничение скорости, аутентификация, завершение SSL, преобразование запросов/ответов, ведение журнала. Kong, AWS API Gateway, Nginx (как шлюз), Traefik. BFF (Backend для Frontend): отдельный шлюз для каждого типа клиента. Sidecar proxy (Envoy): прокси для каждой службы. | API-шлюз и сервисная сетка: шлюз = пограничный, сервисная сетка = внутренняя. |
