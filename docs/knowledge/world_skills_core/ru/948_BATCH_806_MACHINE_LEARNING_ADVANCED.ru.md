# BATCH 806: Machine Learning — Advanced Algorithms

**KnowledgeUnits:** 19
**Namespace:** `mladvanced.ops.*`
**Scope:** deep_learning, CNN, transformer, GAN, reinforcement_learning, GPT_architecture, BERT, embedding_spaces, multimodal

| ID | KnowledgeUnit | Тип | Суть | Практический смысл |
|---|---|---|---|---|
| mladvanced.ops.backpropagation | Обратное распространение ошибки — Алгоритм | METHOD | Обратное распространение: вычислить ∂L/∂w через цепное правило. Проход вперед → вычисление потерь. Обратный проход: ∂L/∂w = ∂L/∂a × ∂a/∂z × ∂z/∂w. Градиентный спуск: w ← w - η × ∂L/∂w. Исчезающий градиент: в сетевых сетях при сигмовидной форме/tanh. ReLU: исчезающий звук. BatchNorm: ещё один метод. | Взрывной градиент: обрезка градиентов (gradient clipping). |
| mladvanced.ops.cnn_architecture | CNN — Сверточная архитектура | METHOD | Свёрточные нейронные сети: Свёрточный слой: карты признаков через фильтры (обучаемые). Объединение: max-pool (пространственное понижение разрешения). Сглаживание → FC → вывод. Рецептивное поле: поле зрения нейрона. Глубина: # каналов. Пропустить соединения (ResNet): разрешить обучать более 100 слоев. Пакетная норма + отсев. | ResNet: Он и др. 2015. Пропустить соединения → решение с исчезающим градиентом. |
| mladvanced.ops.transformer_attention | Трансформер — Внимание к себе | METHOD | Механизм самообслуживания: Q = XWQ, K = XWK, V = XWV. Внимание(Q,K,V) = softmax(QKᵀ/√dk) × V. Мультиголовка: несколько параллельных головок внимания. Позиционное кодирование: синусоидальное или обученное. Кодер-декодер, только кодер (BERT) и только декодер (GPT). | Квадратичная сложность attention: O(n²d). FlashAttention: более эффективный для длинных последовательностей. |
| mladvanced.ops.bert_pretraining | БЕРТ — Предварительная подготовка | METHOD | BERT (представления двунаправленного кодировщика от преобразователей): моделирование языка в масках (MLM): 15% токенов маскируется. Прогнозирование следующего предложения (NSP). Двунаправленный контекст: наблюдает слева и справа. Точная настройка: на последующих задачах с деревянным линейным слоем. РОБЕРТа: без NSP + большая партия. | GPT против BERT: GPT = авторегрессия (однонаправленная), BERT = двунаправленная. |
| mladvanced.ops.gpt_architecture | ГПТ — Архитектура | METHOD | GPT (Генераторный предварительно обученный преобразователь): преобразователь только для декодера. Авторегрессия: прогнозирует следующий токен. Предварительное обучение: причинно-языковое моделирование. Малое количество кадров, ноль: через подсказку (без обновления градиента). Эмерджентные способности: при масштабировании. Обучение в контексте. GPT-4 → GPT-o1 → GPT-4.5 → Клод Клод Сонет 4.6: размер модели. | Instruction tuning (RLHF): выравнивание с человеческими предпочтениями. |
| mladvanced.ops.gan_training | GAN — динамика обучения | METHOD | Генеративно-состязательные сети: Генератор (G) против Дискриминатора (D). G попытаться обмануть D. D попробовать образец настоящего против фейка. Игра Минмакс. Режим коллапса: G продолжается только несколько режимов. WGAN (Wasserstein GAN): коллапс режима экономики. DCGAN, StyleGAN, ПроГАН. Условный GAN: генерация по условию. | FID (Fréchet Inception Distance): метрика качества изображений GAN. |
| mladvanced.ops.reinforcement_learning_q | Обучение с подкреплением — Q-Learning | METHOD | Q-обучение: Q(s,a) = R + γ × max_a' Q(s', a'). Уравнение Беллмана. ε-жадное исследование. DQN (Deep Q-Network, DeepMind 2015): нейросеть аппроксимирует Q-функцию. Опыт буфера воспроизведения. Целевая сеть. Градиент политики (REINFORCE): напрямую оптимизирует политику. | Компромисс «разведка vs эксплуатация»: основная проблема RL. |
| mladvanced.ops.ppo_actor_critic | ППО — Актер-критик | METHOD | Оптимизация проксимальной политики (PPO): градиент политики + отсечение для стабильности. Актер: π(а | с, θ). Критик: V(s, φ). Функция преимущества A(s,a). Цель отсечения: min(rt × At, clip(rt, 1-ε, 1+ε) × At). Стабильное обучение. RLHF: обучение с подкреплением на основе обратной связи с человеком = PPO + модель вознаграждения. | OpenAI InstructGPT: RLHF с PPO = более полезная модель. |
| mladvanced.ops.embedding_word2vec | Вложения — Word2Vec | METHOD | Word2Vec: Skip-gram vs CBOW. Skip-gram: предсказать контекст из слова. CBOW: наоборот. Negative sampling: эффективное обучение. Свойства: «King - Man + Woman = Queen» (аналогии). GloVe: матрица совместной встречаемости. FastText: символьные n-граммы. Contextual embeddings (BERT): зависит от контекста. | Статические вложения vs контекстные: «банк» = одно представление (статическое) vs разное (контекстное). |
| mladvanced.ops.diffusion_models | Модели диффузии — DDPM | METHOD | Диффузионные модели (ДДПМ, 2020): Прямой процесс: запись Гауссова шума поэтапно. Реверс: нейросеть предсказывает шум → вычитает → последовательно изображение. Соответствие очков. Скрытая диффузия (стабильная диффузия): в скрытом пространстве. DALL-E 3: преобразование текста в изображение. | Diffusion vs GAN: более стабильное обучение, лучшее diversity. Медленнее inference. |
| mladvanced.ops.vision_transformer | Vision Transformer — ВиТ | METHOD | Vision Transformer (ВиТ, 2021): Изображение → патчи → сведение → линейная проекция → кодер-трансформер. Никаких извилин. Позиционирование вложений. Токен [CLS] → классификация. Жажда данных: требуется много данных или предварительное обучение (ImageNet-21k, JFT). DeiT: эффективное использование данных ViT. | ViT превосходит CNN по масштабам. CNN лучше с меньшим количеством данных. |
| mladvanced.ops.contrastive_learning | Контрастное обучение — CLIP | METHOD | Контрастное обучение: учить похожие встраивания + разные по отдельности. CLIP (OpenAI): пары изображение + текст → общее пространство для встраивания. Потеря InfoNCE. SimCLR: визуальный элемент с самоконтролем. БЁЛ: без отрицательных примеров. МоКо. Последующие задачи: нулевая классификация. | Приложения CLIP: классификация изображений с нулевым кадром, сопоставление текста с изображением DALL-E. |
| mladvanced.ops.foundation_model | Модели фундамента — масштаб | METHOD | Фундаментальные модели: предварительно обученные на больших данных → точно настроенные для последующей обработки. Появляющиеся возможности при масштабировании (>10^23 FLOP). Законы масштабирования шиншилл: оптимум данных/параметров. GPT-4: обучение токенов ~1,8 трлн. LLaMA 3: открытый исходный код. | Закон о шиншиллах: Hoffmann et al. 2022. Оптимально: N параметров = 20×N токенов. |
| mladvanced.ops.multimodal_llm | Мультимодальный — Интеграция LLM | METHOD | Мультимодальные LLM: GPT-4V, Gemini 1.5, Claude 3 Vision. Кодировка изображения: ViT → визуальные токены → вход LLM. Видео: временная выборка. Аудио: кодировщик, похожий на шепот. Текст + Изображение: совместное пространство для встраивания. Цепочка мыслей мультимодальная. | Модные слияния: раньше отдельные модели для каждой модальности → теперь единый преобразователь. |
| mladvanced.ops.quantization_llm | Квантование LLM — GPTQ | METHOD | Квантизация LLM для вывода: GPTQ (Frantar, 2022): квантование после обучения, INT4, одноразовый проход через данные. BitsAndBytes: 4-битный (NF4). GGUF (llama.cpp): вывод ЦП. AWQ (квантование веса с учетом активации): повышенная точность. ExLlama: быстрый 4-битный графический процессор. | 4-битное квантование: модель 70B → 35 ГБ (по сравнению со 140 ГБ FP16). |
| mladvanced.ops.rag_pipeline | RAG — Поисковая дополненная генерация | METHOD | RAG (дополненная генерация извлечения): запрос → кодирование → извлечение из векторной базы данных → добавление контекста к LLM → создание. База данных векторов: Chroma, Pinecone, Weaviate, FAISS. Плотный поиск: би-энкодер. Редко: БМ25. Гибридный. Реранжирование: кросс-кодер. HyDE (гипотетические встраивания документов). | RAG vs Fine-tuning: RAG для динамических данных, FT для стиля/поведения. |
| mladvanced.ops.moe_architecture | Смесь экспертов — МО | METHOD | Смесь экспертов (MoE): несколько expert networks + gating network выбирает. Sparse MoE: только top-K эксперты активируются (эффективность). Switch Transformer (Google, 2022): один эксперт. GPT-4: предположительно MoE. Mixtral: 8×7B но активирует 2 из 8. Inference: только часть параметров. | MoE trade-off: больше параметров (capacity) при сниженных FLOPs per token. |
| mladvanced.ops.graph_network | Графовая нейронная сеть — типы | METHOD | Графовые нейронные сети: GCN (Сверточная сеть графов): h = σ(D⁻¹/² A D⁻¹/² XW). GAT (Внимание): взвешивание соседей. GraphSAGE: выборка + агрегация. MPNN: передача сообщений. Прогнозирование ссылок, классификация узлов, классификация графов. Гетерогенные графы: разные типы узлов/рёбер. | Молекулярная графика: атомы = узлы, связи = ребра → открытие лекарств. |
| mladvanced.ops.sparse_attention | Эффективное внимание — редкие шаблоны | METHOD | Эффективный Внимание: Редкое Внимание (Child et al. 2019): только часть пар. Лонгформер: скользящее окно + глобальный. BigBird: случайный + оконный + глобальный. Линформер: низкоранговая аппроксимация O(n). FlashAttention (Дао и др., 2022 г.): CUDA с поддержкой ввода-вывода → быстрота и эффективность использования памяти. Внимание к групповым запросам (GQA): LLaMA 2+. | Flash Внимание: ускорение в 2-4 раза, меньше памяти. Стандарт для производства LLM. |
