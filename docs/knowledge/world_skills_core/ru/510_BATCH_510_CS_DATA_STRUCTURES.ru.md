# BATCH 510: Computer Science — Data Structures

**KnowledgeUnits:** 43  
**Namespace:** `cs.data_structures.*`  
**Scope:** arrays, linked_lists, stacks, queues, hash_tables, binary_trees, bsts, heaps, tries, graphs, skip_lists, union_find, segment_trees.

| ID | KnowledgeUnit | Тип | Суть | Практический смысл |
|---|---|---|---|---|
| cs.data_structures.array | Множество | INVARIANT | Массив — непрерывный блок памяти с доступом по индексу O(1). Фиксированный или динамический размер. | Базовое хранилище. CPU-кэш оптимизирован под последовательный доступ к массивам. |
| cs.data_structures.dynamic_array | Динамический массив | VARIANT | Динамический массив (vector, ArrayList): удвоение размера при заполнении. Амортизированное O(1) добавления. | Стандарт: Python list, C++ vector, Java ArrayList. Геометрическое расширение: амортизация O(1). |
| cs.data_structures.linked_list | Связанный список | VARIANT | Связный список: узлы с данными и ссылкой на следующий. Вставка/удаление O(1) (при наличии итератора), доступ O(n). | Стек, очередь без переаллокации. Ядро Linux (list_head). Не кэш-дружественен (указатели разбросаны). |
| cs.data_structures.doubly_linked_list | Двусвязный список | VARIANT | Двусвязный список: ссылки вперёд и назад. Удаление узла по ссылке O(1) без знания предыдущего. | Основа LRU-кэша, навигация вперёд-назад в браузере, undo/redo. |
| cs.data_structures.stack | Куча | INVARIANT | Стек: LIFO (Last In — First Out). Операции: нажать, поп, заглянуть за O(1). | Стек вызовов, парсинг выражений, undo, обход графа в глубину (DFS), backtracking. |
| cs.data_structures.queue | Очередь | INVARIANT | Очередь: FIFO (First In — First Out). Операции: постановка в очередь, удаление из очереди, просмотр O(1). | Планировщики задач, BFS (обход графа), буферы сообщений, очереди печати. |
| cs.data_structures.deque | Deque (двусторонняя очередь) | VARIANT | Дек: вставка и удаление с обоих концов за O(1). Реализуется кольцевым буфером или двусвязным списком. | Python collections.deque. Скользящее окно, алгоритм «максимум в окне» за O(n). |
| cs.data_structures.priority_queue | Приоритетная очередь | VARIANT | Очередь с приоритетом: извлечение элемента с максимальным/минимальным приоритетом за O(log n). Реализуется кучей. | Планировщики (OS scheduler), алгоритм Дейкстры, кодирование Хаффмана, обработка событий. |
| cs.data_structures.hash_table | Хэш-таблица | INVARIANT | Хеш-таблица: ключ → значение через хеш-функцию. Среднее O(1) для вставки, удаления, поиска. | Python dict, Java HashMap, C++ unordered_map. Кэши, индексы БД, распространены. |
| cs.data_structures.hash_collision | Разрешение хеш-коллизий | PROCESS | Разрешение коллизий: цепочки (связный список в бакете), открытая адресация (linear/quadratic probing, double hashing). | Python: открытая адресация + случайный probing. Java: цепочки → красно-чёрное дерево при >8 коллизиях на бакет. |
| cs.data_structures.load_factor | Коэффициент нагрузки | VARIANT | Коэффициент заполнения α = n/m. При превышении порога (обычно 0.75) — рехеширование (удвоение размера). | Компромисс память-время. Слишком высокий α — больше коллизий. Слишком низкий — перерасход памяти. |
| cs.data_structures.perfect_hashing | Идеальное хеширование | METHOD | Совершенное хеширование: O(1) worst-case поиск. Двухуровневое: первичная функция + вторичные без коллизий. | Статические словари (ключи известны заранее). Компиляторы (ключевые слова языка). |
| cs.data_structures.bloom_filter | Фильтр Блума | METHOD | Вероятностная структура: k хеш-функций, битовый массив. «Нет» — точно, «возможно да» — с вероятностью ошибки. Память O(m) бит. | Проверка принадлежности к множеству с экономией памяти. БД, браузеры (безопасный просмотр Chrome). |
| cs.data_structures.cuckoo_hashing | Кукушка Хеширование | METHOD | Две хеш-функции, два бакета. Вставка «выселяет» элемент в другой бакет. O(1) worst-case поиск. | Теоретическая гарантия. Используется в высоконагруженных системах. |
| cs.data_structures.consistent_hashing | Согласованное хеширование | METHOD | Добавление/удаление корзин меняет отображение лишь K/n ключей (а не всех). Кольцевое хеш-пространство. | Шардирование в распределённых системах: DynamoDB, Cassandra, CDN, распределённые кэши. |
| cs.data_structures.binary_search_tree | Двоичное дерево поиска | INVARIANT | BST: левое поддерево ≤ корень ≤ правое. Операции O(h) где h — высота. | Основа для TreeMap/TreeSet. Сбалансированные BST дают O(log n). |
| cs.data_structures.avl_tree | Дерево АВЛ | METHOD | Строго сбалансированное BST: | высота(слева)-высота(справа) | ≤ 1. Балансировка вращениями после вставки/удаления. O(log n). | Первая самобалансирующаяся BST (1962). Применяется где критичен поиск (чаще, чем вставки). |
| cs.data_structures.red_black_tree | Красно-черное дерево | METHOD | Красно-чёрное дерево: менее строгий баланс, но O(log n) гарантировано. Правила цвета, не более 2 поворотов за вставку. | std::map в C++, TreeMap в Java, Completely Fair Scheduler в Linux, epoll. |
| cs.data_structures.b_tree | B-дерево | INVARIANT | B-дерево с большим числом ключей в узле (fanout). Минимизация дисковых операций. Ветвление B~100-1000. | Все реляционные БД (индексы), файловые системы (NTFS, HFS+, btrfs). |
| cs.data_structures.b_plus_tree | B+ Дерево | VARIANT | B+ дерево: все данные в листьях, внутренние узлы — только ключи. Листья связаны списком для range query. | Стандарт для индексов БД: MySQL InnoDB, PostgreSQL. Эффективные range scan. |
| cs.data_structures.heap | Двоичная куча | INVARIANT | Двоичная куча: родитель ≤ детей (min-heap) или ≥ (max-heap). Хранится в массиве: дети 2i+1, 2i+2. | Очередь с приоритетом, heapsort, алгоритм Дейкстры, медиана потока. |
| cs.data_structures.fibonacci_heap | Куча Фибоначчи | METHOD | Более быстрая amortized вставка O(1) и decrease-key O(1) (амортизированно). Высокая константа. | Теоретически улучшает Дейкстру и Прима до O(E + V log V). Практически — бинарная куча проще. |
| cs.data_structures.binomial_heap | Биномиальная куча | VARIANT | Набор биномиальных деревьев, объединяемых двоичным сложением. merge за O(log n). | Теоретическая структура. Полезна для persistent (неизменяемых) реализаций очередей. |
| cs.data_structures.trie | Три (дерево префиксов) | INVARIANT | Дерево префиксов: каждый узел — символ, пути — строки. Поиск за O(L) где L — длина ключа. | Автодополнение (autocomplete), орфография, IP-маршрутизация (radix tree), поиск префиксов. |
| cs.data_structures.suffix_trie | Суффикс Trie/Дерево | METHOD | Дерево суффиксов: компактное представление всех суффиксов строки. Поиск подстроки за O(m). Ukkonen — построение за O(n). | Поиск подстрок (биоинформатика), сжатие данных, поиск повторов, longest common substring. |
| cs.data_structures.graph_adjacency | Представления графов | VARIANT | Матрица смежности (M[i][j], O(V²) память) vs список смежности (O(V+E)). | Список для разреженных графов, матрица для плотных и быстрого теста ребра. |
| cs.data_structures.skip_list | Пропустить список | METHOD | Вероятностная структура: отсортированный список с «экспресс-линиями» наверх. O(log n) среднее. Проще балансировки. | Redis sorted set, LevelDB memtable. Быстрая реализация конкурентного доступа. |
| cs.data_structures.disjoint_set | Объединение непересекающихся множеств (DSU) | VARIANT | Система непересекающихся множеств: union, find с эвристиками (ранг, сжатие путей). O(α(n)) ≈ O(1). | Алгоритм Краскала (MST), связность графа, эквивалентность, сетевые компоненты. |
| cs.data_structures.segment_tree | Дерево сегментов | METHOD | Дерево отрезков: запросы на интервалах [l,r] (сумма, min, gcd) и точечные обновления за O(log n). | RMQ (Range Minimum Query), задачи на интервалах, 2D-деревья, вычислительная геометрия. |
| cs.data_structures.fenwick_tree | Фенвик-Три (БИТ) | VARIANT | Бинарное индексированное дерево: префиксные суммы с обновлениями O(log n). Компактнее сегментного дерева. | Задачи на префиксные суммы с обновлениями: статистика, инверсии, ранги в потоке. |
| cs.data_structures.sparse_table | Разреженная таблица | METHOD | Предподсчёт min/max/gcd для всех степеней двойки. Запрос за O(1), построение O(n log n), неизменяемый массив. | RMQ для статических данных. Быстрее сегментного дерева для запросов без обновлений. |
| cs.data_structures.van_emde_boas | Дерево Ван Эмде Боас | METHOD | Дерево ван Эмде Боаса: операции с целыми ключами за O(log log U) где U — размер универсума. | Экстремально быстрый поиск для целых. Применение: сетевые маршрутизаторы, IP lookup. |
| cs.data_structures.rope | Веревка | METHOD | Сбалансированное дерево для эффективных операций со строками: конкатенация, вставка/удаление подстрок за O(log n). | Текстовые редакторы для больших файлов. Emacs buffer gap, Xi editor (Google) — родственная идея. |
| cs.data_structures.persistent_structure | Постоянные структуры данных | VARIANT | Неизменяемая структура данных: операции создают новую версию, старая доступна. Через копирование путей (path copying). | Функциональное программирование (Clojure, Scala). Git (MERGE через persistent tree). Undo/redo. |
| cs.data_structures.intrusive_list | Навязчивый список | VARIANT | Интрузивный список: указатели встроены в саму структуру данных (не аллокация узлов отдельно). | Ядро Linux (list_head). Нет аллокаций для узлов → предсказуемая производительность, кэш-дружественность. |
| cs.data_structures.circular_buffer | Круговой буфер | VARIANT | Кольцевой буфер: фиксированный массив + указатели head/tail. O(1) enqueue/dequeue без переаллокации. | Очереди фиксированного размера, аудио/видео буферы, скользящие окна, producer-consumer. |
| cs.data_structures.dancing_links | Танцевальные ссылки | METHOD | Техника Кнута (Algorithm X) для точного покрытия: двусвязные списки с быстрым удалением/восстановлением узлов. | Решение судоку, головоломок, задачи упаковки. DLX — Dancing Links. |
| cs.data_structures.treap | Треп | METHOD | Treap (Tree+Heap): BST по ключу + heap по случайному приоритету. Ожидаемая высота O(log n). Простая балансировка split/merge. | Изящная реализация BST в функциональном стиле. Основа для неявного декартова дерева. |
| cs.data_structures.splay_tree | Распространенное дерево | METHOD | Расширяющееся дерево: каждый доступ «поднимает» узел в корень (splay). Амортизированное O(log n). Адаптивно к частым запросам. | Кэширование, адаптивные структуры. Часто запрашиваемые элементы ближе к корню. |
| cs.data_structures.lru_cache | LRU-кэш | METHOD | Least Recently Used кэш фиксированного размера: двусвязный список + hash map. O(1) get/put. Вытеснение наиболее давнего. | Кэши процессора, веб-серверы (Redis as LRU), БД, браузеры, CDN. |
| cs.data_structures.lfu_cache | LFU-кэш | METHOD | Least Frequently Used: вытеснение наименее частого. Счётчики частоты + min-heap или frequency list. Сложнее LRU. | Кэширование с учётом частоты. Защита от вытеснения полезных редко используемых элементов. |
| cs.data_structures.succinct_structure | Краткие структуры данных | METHOD | Компактные структуры: дерево в 2n+o(n) битах вместо указателей. Rank/Select операции. | Геномные базы данных, сжатые индексы текста (FM-index в BWA), большие графы. |
| cs.data_structures.disjoint_set_per_persistence | Постоянный союз-Найти | VARIANT | Неизменяемая версия DSU с сохранением истории версий. Через persistent array. | Анализ временных срезов графа, отладка алгоритмов, undo операций в редакторах. |
