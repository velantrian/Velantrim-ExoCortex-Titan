# Temporary ARM-03 branch note

This branch rebuilds selective-memory candidate extraction from current `main`. The final
validated result will be folded into `docs/ai/WORK_LOG.md` before merge; this temporary
note prevents pre-CI claims from being written into the authoritative work log.
